# MAP-E（OCNバーチャルコネクト）＆ PPPoE 仮想ISPエミュレーション環境 設計仕様書

## 0. 目的・前提

本環境の稼働パラメータはすべて [map-e.conf](map-e.conf) および [pppoe.conf](pppoe.conf) に一元定義されており、運用要件に応じて任意の値にカスタマイズして動作させることが可能です。本仕様書では以下のパラメータを共通の基準値として使用して解説します。

### 基準パラメータ一覧

| 分類 | 変数名 | 設定値 | 説明 |
|---|---|---|---|
| **管理系** | `MGT_IF` | `eth0` | 管理用物理インターフェース |
| | `MGT_IP` | `192.168.0.10/24` | 管理用 IPv4 アドレス |
| | `MGT_GW` | `192.168.0.1` | 管理用デフォルトゲートウェイ |
| | `SYSTEM_DNS` | `192.168.0.1 1.1.1.1` | システム用 DNS |
| | `SYSTEM_NTP` | `192.168.0.1 ntp.example.jp` | システム用 NTP |
| | `DOMAIN` | `example.com` | ドメイン名 |
| | `WEB_DASHBOARD_PORT` | `1600` | Web 統合ダッシュボード待受ポート |
| | `NAT_ENABLED` | **`0`** | **外部接続 NAT (Central NAT) のデフォルト設定（0: 無効）** |
| **WAN / MAP-E 基幹** | `MAPE_IF` | `eth1` | WAN側トランク物理インターフェース |
| | `BR_IF` | `dummy0` | トンネル終端インターフェース (Linux dummy デバイス) |
| | `PROV_IF` | `dummy1` | 共通サービス基盤インターフェース (Linux dummy デバイス) |
| | `BASE_SUBNET` | `2001:db8` | 基幹 IPv6 プレフィックス (RFC 3849) |
| | `BR_PREFIX` | `${BASE_SUBNET}:aaaa` | BR プレフィックス (`2001:db8:aaaa::/96`) |
| | `BR_IPV4_ADDR` | `192.0.2.1` | BR IPv4 アドレス (RFC 5737 TEST-NET-1, hex: `c000:0201`) |
| | `BR_IPV6` | `2001:db8:aaaa::c000:0201` | BR の完全 IPv6 アドレス (自動算出) |
| | `BR_IPV4_POOL` | `198.51.0.0/16` | **動的 IPv4 プール（※アドレス衝突防止のため /16 固定）** |
| | `MAPE_PROV_PREFIX` | `${BASE_SUBNET}:9999` | 共通サービス基盤プレフィックス (`2001:db8:9999::/64`) |
| | `MAPE_DNS_IP` | `2001:db8:9999::53` | DNS サーバー IPv6 アドレス |
| | `MAPE_NTP_IP` | `2001:db8:9999::123` | NTP サーバー IPv6 アドレス |
| | `MAPE_PROV_IP` | `2001:db8:9999::ff` | provisioning-server IPv6 アドレス |
| | `MAPE_DOMAIN_SEARCH` | `map.example.ad.jp` | CE 通知用ドメインサーチリスト |
| **方式別 VLAN & 空間** | `SLAAC_DYN_VLANS` | `101 102` | ① SLAAC動的 (代表接続例: **VLAN 101**, IPv6: `2001:db8:1101::/64`, BR: `::ff0a`, Rule `/40`, EA-bits 16bit) |
| | `PD_DYN_VLANS` | `103 104` | ② DHCPv6-PD動的 (代表接続例: **VLAN 103**, プール: `PD_POOL=2000` → Rule `/40`, 委譲 `/56`, EA-bits 16bit) |
| | `SLAAC_FIX_VLANS` | `105` | ③ SLAAC固定 (代表接続例: **VLAN 105**, IPv6: `2001:db8:3105::/64`, BR: `::ff0d`, Rule `/40`, EA-bits 0) |
| | `PD_FIX_VLANS` | `106` | ④ DHCPv6-PD固定 (代表接続例: **VLAN 106**, 実委譲は②と同一, Rule用: `PD_FIX_POOL=4000` → `/56`, Rule `/40`, EA-bits 0) |
| | `PPPOE_VLANS` | `107` | PPPoE 専用 VLAN (代表接続例: **VLAN 107**) |
| **PPPoE サービス** | `PPPOE_SERVER_BASE_IP` | `203.0.113.0` | 対向サーバー基底 IP (RFC 5737 TEST-NET-3) |
| | `PPPOE_IP_POOL` | `203.0.113.128/25` | PPPoE 動的プール（固定IP未指定ユーザー向け） |
| | `PPPOE_SERVICES` | `Service_A Service_B Internet` | 同時稼働サービス一覧 |
| | `Service_A` | IP: `203.0.113.1`, VRF: `1` (閉域), 収容 VLAN: `101 107` |
| | `Service_B` | IP: `203.0.113.2`, VRF: `1` (閉域), 収容 VLAN: `107` |
| | `Internet` | IP: `203.0.113.3`, VRF: `0` (非VRF / Central NAT対象), 収容 VLAN: `101 107` |

閉鎖環境（Debian Linux）上に、OCNバーチャルコネクト（MAP-E: RFC 7597 / RFC 7598）に準拠したISPプロビジョニングおよび、フレッツ光ネクストにおける PPPoE 接続をエミュレートする統合試験環境を構築する。CE（CEルーター：FortiGate, YAMAHA RTX, NEC IX 等）に対しては、以下の接続方式を独立かつ並行して試験できるようにする。

### 0.1 MAP-E (IPoE) 接続方式（4パターン）

| # | 接続方式 | IPv4割当 | ひかり電話 |
|---|---|---|---|
| ① | SLAAC 動的IPv4 | 動的（共有プールから払い出し） | なし |
| ② | DHCPv6-PD 動的IPv4 | 動的（共有プールから払い出し） | あり |
| ③ | SLAAC 固定IPv4 | 固定（顧客指定IPv4、MACアドレスまたはVLAN番号で指定） | なし |
| ④ | DHCPv6-PD 固定IPv4 | 固定（顧客指定IPv4、MACアドレスまたはVLAN番号で指定） | あり |

4方式はそれぞれ挙動が異なるため混在させず、VLANで分離する（詳細は[2章](#2-wanインターフェースvlan設計)）。

**商用OCNとの相違点と割当方式**：
* **規格準拠の動的ルール配布**：RFC 7598（DHCPv6 Option 94）および RFC 7597（HTTP プロビジョニング）の標準仕様に準拠し、動的IP（①②）の配布ルール（Option 89 / BMR）には事業者共有プール（`BR_IPV4_POOL` の `/16`、例: `198.51.0.0/16`）と EA-bits（16bit）を指定して配布する。CE（ルーター）は規格通り、自身の IPv6 プレフィックスの EA-bits から個別 IPv4 アドレス（`/32`）および PSID（利用可能ポート範囲）を自己計算する。
* **BR（Debian）側の 1台1IP 簡略化ルーティング**：試験の簡便化および高いスループットを維持するため、BR側ではポート制限（NAPT）を行わず、CEが自己計算した個別 IPv4 アドレス（`/32`）宛てのトンネルルート（`ip route add <個別IPv4>/32 dev mpe-common encap ip6...`）をカーネルに自動登録して 1台1IP（ポート制限なし・全ポート利用）で通信させる。
* **ダッシュボードの表示**：Web 統合ダッシュボードのクライアント一覧では、この「実際に CE へ割り当てられ通信している個別 IPv4 アドレス」を表示する。

CEは動作確認用に FortiGate, YAMAHA RTX および NEC IX シリーズルーターを使用する想定。

**Option 94生成方式**：動的接続方式（①②）は、規格（RFC 7598）に準拠した完全な Option 94 静的 hex（`/16` プール、Option 90/89/93）を `libdhcp_flex_option.so` の `supersede` により配布する。一方、固定接続方式（③④）は「顧客が指定した特定の個別固定IPv4アドレス（`/32` または `/28` 等）をそのままCEへ割り当てたい」という要件に応じ、Kea標準のHost Reservationごとの `option-data` 個別指定機能で完全固定 hex を直接指定する方式を採用する（詳細は[4章](#4-option-94map-eルール生成ロジックflex-optionhost-reservation併用方式)・[5章](#5-ipv4アドレス算出ロジックflex-optionhost-reservation併用方式)・[13章](#13-flex-option-詳細仕様)を参照）。

---

## 1. 全体設計方針

* **Debian 1台で完結**：BR（Border Relay：MAP-E終端ルーター）および各種サーバー機能を1台のDebian（Linux）で稼働させる。
* **Docker コンテナによるサービス集約**：PPPoE サーバー、DNS（BIND9）、DHCPv6（Kea）、RA（radvd）、NTP（chrony）、Syslog（AxoSyslog）、MAP-E 設定配布（provisioning-server）、および Web 統合ダッシュボードは Docker コンテナとして一元管理・運用する。
* **PPPoE マルチサービス並行エミュレーション**：同一のトランク物理インターフェース上で、MAP-E と並行して複数の PPPoE サービス（マルチセッション・VRF閉域網分離・自動フェイルオーバー）をコンテナ稼働させる。
* **一元管理**：パラメータ定義は `map-e.conf` および `pppoe.conf` に集約し、スクリプト群（`generator/`）で各設定ファイルを自動生成する。
* **パラメータの汎用化**：設計書内の具体例・パラメータは標準的な基準値（RFC 3849 / RFC 5737 準拠の予約アドレス空間）で統一し、実環境の秘匿性を確保する。

---

## 2. WANインターフェース・VLAN設計

物理インターフェース `<MAPE_IF>`（例: `eth1`）上に802.1Q VLANインターフェースを作成し、方式ごとにVLANを分離する。

```
<MAPE_IF> (eth1)
├── eth1.101 ── SLAAC動的   (① ひかり電話なし・動的)
├── eth1.102 ── SLAAC動的   (① ひかり電話なし・動的)
├── eth1.103 ── DHCPv6-PD動的 (② ひかり電話あり・動的)
├── eth1.104 ── DHCPv6-PD動的 (② ひかり電話あり・動的)
├── eth1.105 ── SLAAC固定   (③ ひかり電話なし・固定)
├── eth1.106 ── DHCPv6-PD固定 (④ ひかり電話あり・固定)
└── eth1.107 ── PPPoE専用   (PPPoE マルチサービス検証用)
```

※ **PPPoE との VLAN 共有**: `PPPOE_VLANS` に指定された VLAN（例: `107`）に加え、`make generate` 実行時に MAP-E 用の全 VLAN（`101〜106`）が自動的に PPPoE サーバーのバインド対象（`VLAN_IFS`）へ合算されるため、すべての VLAN で MAP-E と PPPoE の双方を検証可能である。

* **各方式に複数VLANを割り当て可能**：`map-e.conf` の `SLAAC_DYN_VLANS`、`PD_DYN_VLANS` 等にスペース区切りで複数のVLAN IDを指定できる。
* **1VLAN=1CE原則**：試験の再現性を担保するため、原則として各VLANにはCE 1台のみを接続する。同一の接続方式で複数台のCEルーターを同時に接続して並行試験を行う場合は、台数分のVLANを用意して収容する（例: SLAAC動的で2台同時に接続・検証する場合は VLAN 101 と VLAN 102 にそれぞれ1台ずつ接続する）。

---

## 3. IPv6/IPv4 アドレス空間設計

基準パラメータ（`BASE_SUBNET="2001:db8"`, `BR_IPV4_POOL="198.51.0.0/16"`）に基づくアドレス設計を以下に示す。

### 3.1 IPv6アドレス空間

```
2001:db8::/32 (基幹IPv6プレフィックス: BASE_SUBNET)
│
├── 2001:db8:aaaa::/96  ── BRプレフィックス (BR_PREFIX)
│   └── 2001:db8:aaaa::c000:0201 ── BR IPv6完全アドレス (BR_IPV6: dummy0)
│
├── 2001:db8:1000::/40  ── SLAAC動的用空間
│   ├── 2001:db8:1101::/64 ── VLAN 101 (BR: 2001:db8:1101::ff0a)
│   └── 2001:db8:1102::/64 ── VLAN 102 (BR: 2001:db8:1102::ff0a)
│
├── 2001:db8:2000::/40  ── DHCPv6-PD動的用プール (PD_POOL: Keaで/56ずつ委譲)
│   ├── 2001:db8:2000:0100::/56 ── クライアント1への委譲例 (EA-bits: 0x0100 -> 198.51.1.0)
│   └── 2001:db8:2000:0001::/56 ── クライアント2への委譲例 (EA-bits: 0x0001 -> 198.51.0.1)
│
├── 2001:db8:3000::/40  ── SLAAC固定用空間
│   └── 2001:db8:3105::/64 ── VLAN 105 (BR: 2001:db8:3105::ff0d)
│
├── 2001:db8:4000::/40  ── DHCPv6-PD固定のOption 94用空間 (PD_FIX_POOL)
│   └── (実委譲は 2001:db8:2000::/40 から動的に払い出し、Option 94のRule IPv6 Prefixに本値を設定)
│
└── 2001:db8:9999::/64  ── 共通サービス基盤 (PROV_IF: dummy1)
    ├── 2001:db8:9999::53  ── DNS (BIND9)
    ├── 2001:db8:9999::123 ── NTP (chrony)
    └── 2001:db8:9999::ff  ── MAP-E プロビジョニングサーバー
```

### 3.2 IPv4アドレス空間

```
198.51.0.0/16 (動的IPv4プール: BR_IPV4_POOL ※/16固定)
├── 198.51.x.y/32 ── 動的MAP-Eクライアント（①SLAAC動的 / ②PD動的）へ1台1IP割り当て (EA-bits 16bitで直結)
│
203.0.113.0/24 (固定IPv4 / PPPoE空間)
├── 203.0.113.1  ── PPPoE サーバー Service_A
├── 203.0.113.2  ── PPPoE サーバー Service_B
├── 203.0.113.3  ── PPPoE サーバー Internet
├── 203.0.113.10/32 ── 固定MAP-E / PPPoE クライアント割当例 1
└── 203.0.113.20/32 ── 固定MAP-E / PPPoE クライアント割当例 2
```

---

## 4. Option 94（MAP-Eルール）生成ロジック（flex-option／Host Reservation併用方式）

### 4.1 RFC 7598 Option 94 の基本構造

DHCPv6 Option 94（`OPTION_S46_CONT_MAPE`）はコンテナオプションであり、内部に以下のサブオプションを内包する。

1. **Option 90（`OPTION_S46_BR`）**：BR（Border Relay）のIPv6アドレス（16byte）。
2. **Option 89（`OPTION_S46_RULE`）**：MAP-Eマッピングルール（BMR）。
   - `flags` (1byte) + `ea-len` (1byte) + `prefix4-len` (1byte)
   - `Rule IPv4 Prefix` (可変長。動的IPでは `/16`＝2byte、固定IPでは `/32`＝4byte または `/28`＝4byte)
   - `prefix6-len` (1byte) + `Rule IPv6 Prefix` (可変長、`/56`＝7byte または `/64`＝8byte)
3. **Option 93（`OPTION_S46_PORTPARAMS`）**（動的接続方式のみ付与）：
   - `offset-len` (4bit) + `psid-len` (8bit) + `PSID` (16bit)

### 4.2 接続方式別のOption 94生成アプローチ

* **動的接続方式（①②）**：RFC 7598 に完全準拠し、事業者プール（`BR_IPV4_POOL` の `/16`、`ea-len=16`）を静的 hex として `libdhcp_flex_option.so` の `supersede` により配布。CE は受け取ったプール値と自身の IPv6 プレフィックスの EA-bits から、個別 IPv4 アドレスおよび PSID を規格通り自己計算する。
* **固定接続方式（③④）**：`map-e-static-ip.conf` の定義に基づき、Kea の Host Reservation の `option-data` に事前計算した完全固定 hex 値（個別固定IPv4、`ea-len=0`）を直接設定。登録がない CE は固定IPの VLAN に接続しても接続できない（フォールバックせず拒否）。

### 4.3 動的接続における /16 プールおよび EA-bits 16bit 設計の理由

RFC 7597（MAP-E）の規定により、$\text{Rule IPv4 Prefix Length} + \text{EA-bits Length} \le 32$ である必要がある。本環境では全接続方式で Rule IPv6 Prefix Length を **/40（5byte）** に統一し、動的接続の EA-bits 長に **16bit** を採用する。Rule IPv4 プレフィックス長を **`/16` 固定（例: `198.51.0.0/16`）** とすることで、$16 + 16 = 32\text{bit}$ となり、委譲された IPv6 プレフィックスの可変部 16bit がそのまま IPv4 の下位 16bit（第3・第4オクテット）に 1 対 1 でマッピングされる。これにより最大 65,536 台まで IPv4 アドレスの重複なく 1 IP = 1 台で収容できる。

### 4.4 Option 94 の生成構造（動的接続方式）

#### 1. DHCPv6-PD動的（②）
* **Option 90 (BR)**: `005a0010` + `BR_IPV6` (16byte hex)
* **Option 89 (Rule)**: `0059000b` + `00` (flags) + `10` (ea-len 16) + `10` (prefix4-len 16) + `BR_IPV4_POOL上位2byte` + `28` (prefix6-len 40) + `PD委譲プール5byte hex`
* **Option 93 (PortParams)**: `005d000400000000` (offset=0, psid-len=0, psid=0)

#### 2. SLAAC動的（①）
* **Option 90 (BR)**: `005a0010` + `BR_IPV6` (16byte hex)
* **Option 89 (Rule)**: `0059000b` + `00` (flags) + `10` (ea-len 16) + `10` (prefix4-len 16) + `BR_IPV4_POOL上位2byte` + `28` (prefix6-len 40) + `SLAACプール5byte hex`
* **Option 93 (PortParams)**: `005d000400000000` (offset=0, psid-len=0, psid=0)

### 4.5 固定接続方式（③④）の Option 94 Host Reservation 個別指定

`map-e-static-ip.conf` に登録された MAC アドレスに対し、事前計算した完全固定 hex を Host Reservation の `option-data` として埋め込む。

| フィールド | 値 | 備考 |
|---|---|---|
| OPTION_S46_BR (type90, len16) | `BR_IPV6`固定値 | [6章](#6-br内部インターフェース)の生成式で計算済みの固定値 |
| OPTION_S46_RULE ヘッダ flags | `0x00` | |
| ea-len | `0x00` | ③④は1台1IP専有 |
| prefix4-len | `0x20`(32) または `0x1c`(28) | マスク長に応じた値 |
| Rule IPv4 Prefix | **MAC個別の固定IPv4**（`map-e-static-ip.conf`の値をhex変換） | 顧客指定値 |
| prefix6-len | ③:`0x28`(40) ／ ④:`0x28`(40) | 全接続方式 /40 統一 |
| Rule IPv6 Prefix | ③:`${BASE_SUBNET}:${SLAAC_FIX_BR_PREFIX}::`（/40） ／ ④:`${BASE_SUBNET}:${PD_FIX_POOL}::`（/40） | /40 固定値 |

**③のVLAN依存性について**：③(SLAAC固定)はVLANごとにIPv6プレフィックスが異なるため、同一MACのCEでも接続先VLANによってOption 94のIPv6部分（Rule IPv6 Prefix）が変わる。したがって③のreservationはSubnet（VLAN）をまたいで使い回せず、Subnetごとに個別のreservationとして生成する（設計上1VLAN=1CEのため、そのVLANで使用する予定のMAC1件を紐付ける運用となる）。
**④のVLAN非依存性について**：④(PD固定)はRule IPv6 Prefixが共通固定値（`PD_FIX_POOL`）であるため、VLAN（Subnet）に依存しない。reservationはshared-network直下に1回だけ登録すれば、どのPD固定VLANに接続しても同一のOption 94が適用される。

---

## 5. IPv4アドレス算出ロジック（flex-option／Host Reservation併用方式）

Kea（動的方式：flex-option／固定方式：Host Reservation個別指定）と `mape-provisioning-server`（BR側ルーティング注入）の間で一貫した割り当てを行うため、**両者が同一のロジックを、それぞれの言語（Kea式言語・Kea設定 / Perl・Python）で独立に実装**する。共通ライブラリとして計算式そのものを共有することはできない（Kea側は式言語または静的設定、BR側の`mape_calc`はスクリプト言語のため）ので、本章の定義を「唯一の正」とし、両実装がこれに一致することをテストで担保する。

* **PD動的（②）**：`BR_IPV4_POOL`（例: `198.51.0.0/16`、**/16固定**）の上位16bitを固定プレフィックスとし、委譲プレフィックス `/56` から Rule IPv6 Prefix `/40` の差分である bit 40〜55（16bit、第3ヘクステット下位8bit＋第4ヘクステット上位8bit）を EA-bits として抽出する。これを IPv4 の第3・第4オクテットにマッピングして `/32`（`198.51.[EA上位8bit].[EA下位8bit]/32`、例: EA-bits が `0x0001` の場合 `198.51.0.1`、`0x0100` の場合 `198.51.1.0`）を合成する。EA-bits 16bit が全て IPv4 アドレスに使われるため、$\text{PSID長} = 16 - (32 - 16) = 0$ となり、`PSID = 0`（全ポート専有）となる。
* **SLAAC動的（①）**：`BR_IPV4_POOL`（同上、**/16固定**）の上位16bitを固定プレフィックスとし、WANプレフィックス `/64` から同様に bit 40〜55（16bit）を EA-bits として抽出し、IPv4 第3・第4オクテットへマッピングする。
* **固定セグメント（③④共通）**：`map-e-static-ip.conf`は「MACアドレス→顧客指定の固定IPv4」の対応表であり、登録されたMACに対しては完全固定hex（個別固定IPv4、`ea-len=0`、`v6-len=40`）を直接指定する。

**CE IPv6アドレス（データ通信用）の合成**：CEがデータ通信のカプセル化送信元として使うIPv6アドレスをBR側（`mape_calc`）でも同一ロジックで算出し、トンネル対向アドレスとして設定する。割り当てた `IPv4/32` を16進数に変換し、RFC 7597 Section 6.2 に準拠した形式でインターフェースID（後半64ビット）へ埋め込む。本環境では 1 台の CE に 1 つの IPv4 アドレス(/32)を 1:1 で割り当てるため（BR側ポート制限なし）、Interface ID の PSID フィールドは常に `0`（末尾 `0000:0000`）として生成する。

**トンネルデバイス構成**：対向ごとに個別にトンネルを作るのではなく、システム全体で1つの共通デバイス `mpe-common` を共有して動作する。`ip6tnl` の `external` モードを利用し、対向ごとのカプセル化パラメータ（src/dst）はルート単位で `encap ip6` として設定する。

---

## 6. BR内部インターフェース

CE収容用のVLAN群（[2章](#2-wanインターフェースvlan設計)）とは別に、BR（Debian）内部で完結する2つの仮想インターフェースを設ける。物理ポートには接続しない。

| インターフェース | 変数名 | プレフィックス | 用途 |
|---|---|---|---|
| `dummy0` | `BR_IF` | `BR_PREFIX`（`/96`） | MAP-Eトンネル終端（BRのIPv6/IPv4エンドポイント） |
| `dummy1` | `PROV_IF` | `MAPE_PROV_PREFIX`（`/64`） | DNS(`::53`)・NTP(`::123`)・プロビジョニングサーバー(`::ff`)の共通サービス基盤 |

`dummy1` はWAN側の全VLANからルーティングで到達可能とし、DNS/NTP/provisioning-serverはVLANごとに個別のアドレスを持たない（全方式・全VLAN共通の単一アドレスを使う）。各変数の設定例は[12.2](#122-br内部インターフェースmap-e共通)を参照。

**dummy0のIPv6アドレス合成（Option 94 Type 90 / BR IPv6）**：OCN商用仕様に準拠し、RFC 6052形式でIPv4を埋め込んで自動生成する。この計算だけは値を追った方が理解しやすいため、設定例の値での計算例を示す。

* 生成式：`BR_PREFIX（96bit）` + `BR_IPV4_ADDR（32bit、16進変換）`
* 計算例（設定例の値）：`BR_PREFIX = 2001:db8:aaaa::/96`、`BR_IPV4_ADDR = 192.0.2.1`（16進: `c000:0201`）→ `BR_IPV6 = 2001:db8:aaaa::c000:0201`

CEはこのアドレスをトンネル対向（宛先）として認識し、カプセル化パケットを送信する。Debian側も同アドレスを `dummy0` のトンネル`local`アドレスとしてバインドし、カプセル化を解除する。

---

## 7. セグメント・インターフェースの3層分類

| 層 | 内容 |
|---|---|
| **① CE収容インターフェース（アクセスVLAN層）** | BRが各方式のCE群を直接収容し対向通信を行うVLAN群（[2章](#2-wanインターフェースvlan設計)）。SLAAC系はRAでプレフィックス通知しCEが自動アドレス取得、PD系はリンクローカルのみで対向しCEへプレフィックスを委譲。 |
| **② BR内部仮想インターフェース（コア・サービス層）** | `dummy0`（トンネル終端）・`dummy1`（DNS/NTP/provisioning共通サービス）（[6章](#6-br内部インターフェース)）。 |
| **③ CE配下ネットワーク（宅内LAN層）** | CEがIA_PDで取得したプレフィックス（動的または固定の`/56`）をLAN側にさらに分配（通常`/64`）し、宅内端末の通信を模擬する試験対象領域。 |

---

## 8. 動作シーケンス

### ひかり電話なし・動的IPv4（SLAAC、①）
1. CEはSLAACでWAN側IPv6アドレスを取得する（provisioning-serverへの接続用）。
2. DHCPv6のInformation-RequestでDNS・NTP・domain search listを取得する。
3. DNSを引いてprovisioning-serverへアクセスし、MAP-Eトンネル用のIPv6アドレスおよびIPv4アドレスを取得する。
4. provisioning-serverは、払い出した情報に従って対向側のルーティングを自動設定する。
5. CEは受け取ったMAP-E接続用IPv6アドレスでMAP-Eトンネル（ip6tnl）を作成する。
6. CEはMAP-Eトンネル経由で外部IPv4との通信を開始する。
7. （DDNS自動登録）CEがDDNS更新機能（RFC 2136 または FortiGate等の `/nic/update` エンドポイント）を備えている場合、BIND9 または provisioning-server に自身のFQDNを自動登録する。

### ひかり電話あり or ひかりクロス・動的IPv4（DHCPv6-PD、②）
1. RAでリンクローカルのデフォルトルートを流す。
2. DHCPv6（IA_PD）でLAN用プレフィックス、およびDNS・NTP・domain search listを取得する。
3. 以降は①と同様の手順でMAP-Eトンネルを確立する（BR-CE間はリンクローカルアドレスで通信、デフォルトルートはRAを受信したリンクローカルアドレス経由で設定される）。
4. （DDNS自動登録）①と同様、必要に応じてDDNS自動更新を行う。

※RAの動作が①③と②④で異なるため、SLAAC系とDHCPv6-PD系は必ず別VLANにする。

---


## 9. プロジェクト構成

```
map-e_ocn/
├── map-e.conf                   # 中央設定ファイル（全パラメータ定義）
├── map-e.conf.tmpl              # map-e.conf 初期化用テンプレート
├── map-e-static-ip.conf         # 固定IPv4マッピング (MAC→顧客指定IPv4)
├── map-e-static-ip.conf.tmpl    # map-e-static-ip.conf 初期化用テンプレート
├── pppoe.conf                   # PPPoEアカウント・IP・サービス・備考一元定義ファイル
├── pppoe.conf.tmpl              # pppoe.conf 初期化用テンプレート
├── ddns.conf                    # DDNS ホスト名マッピング定義ファイル
├── docker-compose.yml           # Dockerコンテナオーケストレーション定義 (全サービスhostネットワーク)
├── Makefile                     # テンプレート展開・インストール・パッケージ管理
├── 方針.md                      # 本ドキュメント（仕様書）
├── OCN_設定簡単まとめ.md        # リファレンス・設定要約
├── verify_calc.py               # 計算ロジック検証用テストスクリプト
│
├── generator/                   # ビルド・設定生成用スクリプト群
│   ├── MapeCommon.pm            # 共通Perlモジュール (環境変数処理・自動計算)
│   ├── check_config.pl          # 設定ファイル構文・整合性チェック
│   ├── gen_kea_configs.pl       # Kea DHCPv6 設定生成スクリプト
│   ├── gen_bind_configs.pl      # BIND9 設定生成スクリプト
│   ├── gen_system_mod.pl        # システム/ネットワーク設定生成スクリプト
│   ├── gen_radvd_mod.pl         # radvd 設定生成スクリプト
│   ├── gen_chrony_config.pl     # chrony NTP 設定生成スクリプト
│   ├── gen_mape_mod.pl          # mape-provisioning 関連生成スクリプト
│   ├── gen_pppoe_configs.pl     # PPPoE 各種設定および docker-compose.yml 生成スクリプト
│   └── gen_bearded_themes.pl    # Webダッシュボード テーマ生成スクリプト（※テーマ再生成時は git clone https://github.com/BeardedBear/bearded-theme.git をルート直下にクローンして実行）
│
├── nat/                         # Central NAT (外部接続 NAT コンテナ環境)
│   ├── Dockerfile               # iptables 実行用軽量コンテナイメージ定義
│   └── entrypoint.sh            # NAT 転送・MASQUERADE ルール設定スクリプト
│
├── CE-setting/                  # CEルーター(NEC IX, FortiGate, YAMAHA等)の設定例・コマンド集（随時更新）
│
├── pppoe/                       # PPPoE サーバー環境
│   ├── Dockerfile               # カスタム pppoe-server ビルド定義 (denyリストパッチ適用)
│   ├── entrypoint.sh            # PPPoE コンテナ起動スクリプト
│   ├── ip-up / ip-down          # PPP セッション確立/切断フック
│   ├── patches/                 # ソースコードパッチ
│   │   └── pppoe-server-deny-list.patch  # 自動フェイルオーバー・1分間denyリストパッチ
│   └── srv-<Service>/           # サービス別設定ディレクトリ（例: srv-Service_A/ 等）
│       ├── chap-secrets / pap-secrets    # 認証アカウント
│       ├── routes.conf          # 委任プレフィックス等ルーティング定義
│       ├── pppoe-server-options # pppd 起動オプション
│       └── deny-mac.list        # 静的 MAC 拒否リスト
│
├── web-dashboard/               # Web 統合ダッシュボード (Port 1600)
│   ├── Dockerfile
│   ├── app.py                   # エントリーポイント + HTTPサーバー起動 (薄いラッパー)
│   ├── constants.py             # グローバル定数・環境変数解決
│   ├── config_app.py            # AppConfig (ダッシュボード自体の環境変数・定数管理)
│   ├── config_manager.py        # ConfigManager (map-e.conf 等の設定ファイル読み書き・検証)
│   ├── config.py                # 下位互換アダプター
│   ├── utils.py                 # run_cmd / ping / KeaClient / ログ読み込みヘルパー群
│   ├── collectors.py            # PPPoECollector / MAPECollector (データ収集)
│   ├── handlers.py              # DashboardHandler (全 API エンドポイント)
│   └── static/                  # フロントエンド静的ファイル
│       ├── index.html           # ダッシュボード HTML 構造
│       ├── config.html          # 環境設定エディタ HTML 構造（独立画面）
│       ├── css/main.css         # ダッシュボード スタイルシート
│       ├── js/                  # フロントエンド JavaScript (モジュール分割)
│       │   ├── main.js          # 初期化・イベントバインド・ポーリング
│       │   ├── tables.js        # クライアントテーブル描画・ソート
│       │   ├── modals.js        # モーダルダイアログ制御
│       │   ├── config-editor.js # 設定エディタ制御
│       │   ├── theme-manager.js # テーマ管理
│       │   └── bearded-themes.js# テーマ定義
│       ├── tool_ping.html       # Ping ツール
│       ├── tool_routes.html     # ルーティングテーブルビューワー
│       ├── tool_interfaces.html # インターフェース一覧ビューワー
│       └── tool_logs.html       # ログビューワー
│
├── tools/                       # フロントエンド検証・E2Eデバッグ用ツール群 (通常運用時は不要)
│   ├── package.json             # ツール用 npm パッケージ定義 (Playwright, PostCSS 等)
│   ├── validate.mjs             # JavaScript 構文バリデータ
│   ├── validate_css.mjs         # CSS 構文バリデータ
│   └── check_dashboard.mjs      # Playwright によるダッシュボード E2E / コンソールエラー検証スクリプト
│
├── system/                      # ネットワーク設定
│   ├── interfaces               # 生成済み設定 → /etc/network/interfaces
│   └── 99-network-routing.conf  # 生成済み設定 → /etc/sysctl.d/
│
├── radvd/                       # Router Advertisement
│   ├── Dockerfile
│   ├── entrypoint.sh            # radvd コンテナ起動スクリプト
│   └── radvd.conf               # 生成済み設定
│
├── kea-dhcp6/                   # DHCPv6 サーバー
│   ├── kea-dhcp6.conf.tmpl      # テンプレート
│   ├── kea-dhcp6.conf           # 生成済み設定
│   ├── kea-map-e-hook.tmpl      # テンプレート (Kea hookスクリプト。ルーティング注入・削除用)
│   └── kea-map-e-hook           # 生成済み
│
├── bind/                        # DNS (BIND9)
│   ├── named.conf               # BIND9 メイン設定
│   ├── rndc.conf                # rndc 制御設定
│   ├── named.conf.local.tmpl    # テンプレート
│   ├── named.conf.local         # 生成済み
│   ├── db.map.ocn.ad.jp.tmpl    # テンプレート (OCNゾーン)
│   ├── db.map.ocn.ad.jp         # 生成済み
│   ├── db.v6connect.net.tmpl    # テンプレート (v6connectゾーン)
│   ├── db.v6connect.net         # 生成済み
│   └── dynamic/db.<ドメイン>    # 動的 DDNS ゾーンファイル (make generate で自動生成・DDNS動的更新対応)
│
├── chrony/                      # NTP (chrony)
│   ├── Dockerfile
│   ├── entrypoint.sh            # chrony コンテナ起動スクリプト
│   ├── chrony.conf.tmpl         # テンプレート
│   └── chrony.conf              # 生成済み
│
├── syslog-ng/                   # ログ出力・ローテーション設定
│   ├── Dockerfile
│   ├── entrypoint.sh            # syslog-ng コンテナ起動スクリプト
│   ├── syslog-ng.conf           # AxoSyslog メイン設定
│   └── logrotate.conf           # ログローテーション設定
│
└── mape-provisioning-server/    # MAP-E 設定配布・ルーティング
    ├── Dockerfile
    ├── mape-provisioning-server.tmpl          # テンプレート
    ├── mape-provisioning-server               # 生成済み
    ├── mape_calc.tmpl                         # 共通ライブラリテンプレート
    ├── mape_calc                              # 生成済み共通ライブラリ
    ├── server.crt / server.key                # TLS 証明書 (openssl で自動生成)
    └── success_examples.md                    # 配布レスポンス仕様・解説（JSONサンプルは付録C.2参照）
```

インターフェースや各種アドレスは `./map-e.conf` に一元定義し、すべての設定ファイル・スクリプトはこれに従う。

---

## 10. ビルドシステム（Makefile）

**設定生成・テンプレート展開の仕組み**：
* [map-e.conf](map-e.conf) は [Makefile](Makefile) 内で環境変数へエクスポートされ、各生成処理で読み込まれる（`generator/` 配下のスクリプトは [MapeCommon.pm](generator/MapeCommon.pm) の共通処理によりシェル変数展開の解決や派生変数の自動計算を行う）。
* 各設定ファイル・スクリプトの生成は、`generator/` 配下のスクリプト群を呼び出すことで実行される：
  * **プログラムによる直接出力**（テンプレート不使用）：
    * `system/interfaces` および `system/99-network-routing.conf` — [gen_system_mod.pl](generator/gen_system_mod.pl) が `SLAAC_DYN_VLANS` などの変数から VLAN ごとの設定ブロックを動的に組み立てて出力する。
    * `radvd/radvd.conf` — [gen_radvd_mod.pl](generator/gen_radvd_mod.pl) が VLAN 設定やプレフィックス情報を基に直接構成を出力する。
  * **テンプレートファイル（`.tmpl`）の置換展開**：
    * `chrony/chrony.conf` — [gen_chrony_config.pl](generator/gen_chrony_config.pl) が `chrony.conf.tmpl` を読み込み、`SYSTEM_NTP` を複数行の server 記述に変換して埋め込む。
    * `bind/named.conf.local`, `bind/db.map.ocn.ad.jp`, `bind/db.v6connect.net` — [gen_bind_configs.pl](generator/gen_bind_configs.pl) が対応するゾーンテンプレートを置換展開する。
    * `kea-dhcp6/kea-dhcp6.conf` — [gen_kea_configs.pl](generator/gen_kea_configs.pl) が `kea-dhcp6.conf.tmpl` をベースに、`KEA_INTERFACES` や `KEA_SUBNET6_BLOCKS` 等を差し替えて生成する。
    * `kea-dhcp6/kea-map-e-hook` — [gen_kea_configs.pl](generator/gen_kea_configs.pl) が `kea-map-e-hook.tmpl` の中身をそのまま出力する（置換処理なし）。
    * `mape-provisioning-server/` 配下の `mape_calc`, `mape-provisioning-server` — [gen_mape_mod.pl](generator/gen_mape_mod.pl) が各テンプレートファイルの `${変数名}` 部分を環境変数で置換して出力する。
  * **PPPoE 設定および Docker Compose の動的生成**：
    * `pppoe/srv-<Service>/` 配下の各種設定（`chap-secrets`, `pap-secrets`, `routes.conf`, `pppoe-server-options`, `deny-mac.list`）および `docker-compose.yml` — [gen_pppoe_configs.pl](generator/gen_pppoe_configs.pl) が `pppoe.conf` と `map-e.conf` のマルチサービス設定を読み込み、サービスごとにマルチVLAN対応の設定ファイル群と、各コンテナ定義（`central-nat` を含む）を動的に生成する。
  * **動的 DDNS ゾーンファイルの自動生成と DDNS 登録**：
    * `bind/dynamic/db.<ドメイン>` — `make generate_ddns_zones` ターゲットで [gen_bind_configs.pl](generator/gen_bind_configs.pl) が `ddns.conf` からドメイン一覧を取得し、動的更新用ゾーンファイルを生成する。登録された各ドメインは BIND9 の `allow-update` により RFC 2136（`nsupdate`）および FortiGate DDNS（`/nic/update`）からの動的登録を受け付ける。
  * **openssl による直接生成**：
    * `mape-provisioning-server/server.crt` および `server.key` は [Makefile](Makefile) 内で `openssl req` コマンドにより直接生成される。

**Makeターゲット**：

| ターゲット | 実行可能ユーザー | 説明 |
|---|---|---|
| `make` / `make generate` / `make all` | 一般ユーザー (`check-user` が強制) | `map-e.conf` および `pppoe.conf` の定義に基づき、すべての設定ファイル・スクリプト・Docker Compose定義を一括生成 |
| `make check` | root 権限 (`check-root` が強制) | chrony、BIND、radvd、Kea DHCPv6、PPPoE の構文チェックを実行 |
| `make install` | root 権限 (`check-root` が強制) | ホスト側のネットワーク／sysctl設定を反映し、Docker コンテナ群を一括ビルド・起動 |
| `make up` | 一般ユーザー | 生成済みの `docker-compose.yml` に基づき全サービスコンテナを起動 |
| `make down` | 一般ユーザー | 全サービスコンテナを一括停止 |
| `make web-dashboard` | 一般ユーザー | Web 統合ダッシュボードのみを先行起動 |
| `make packages` | root 権限 (`check-root` が強制) | ホスト側の動作確認に必要なツール類を apt-get でインストール |
| `make archive` | 一般ユーザー (`check-user` が強制) | ソースコード一式を `MAP-E.tar.xz` に圧縮アーカイブ化 |
| `make clean` | 制限なし | 生成された設定ファイル群および中間ファイルを削除 |

**実行ユーザー制限（check-user / check-root）**：
* 一般ユーザーでのみ実行できるターゲット (`generate`, `archive`, `up`, `down`, `web-dashboard`) と、root 権限が必須なターゲット (`check`, `install`, `packages`) を Makefile 内の `check-user` / `check-root` タスクで切り分け、誤操作を防ぐ。

**install時の差分検知とサービス反映**：
* `/etc/network/interfaces` は `cmp -s` により生成ファイルと現設定に差分がある場合のみ上書きされ、ifdown/ifup または networking サービス再起動で再ロードされる。
* Docker Compose 定義（`docker-compose.yml`）に基づき、すべてのサービスコンテナをビルド・起動する。

---

## 11. 設定対象ファイル一覧

1. **ネットワーク設定**（`./system/`）
   * `interfaces` — インターフェース設定（管理用、SLAAC系VLAN群、PD系VLAN群、`dummy0`/`dummy1`）
   * `99-network-routing.conf` — カーネルパラメータ（IPv4/IPv6転送、rp_filter無効化、broadcast ping応答）

2. **Router Advertisement**（`./radvd/`）
   * `radvd.conf` — 方式ごとに異なるRA動作
     * SLAAC系VLAN群（①③）：プレフィックス通知あり、SLAAC有効
     * PD系VLAN群（②④）：プレフィックス通知なし、DHCPv6誘導のみ

3. **DHCPv6サーバー**（`./kea-dhcp6/`）
   * `kea-dhcp6.conf` — Kea設定
     * subnet6（SLAAC動的）— プールなし、Information-Request応答のみ。Option 94はSubnet直下のflex-option式で付与（DNS, NTP, domain-search）
     * subnet6（SLAAC固定 各VLAN）— プールなし。`map-e-static-ip.conf`登録済みMACはHost Reservationの`option-data`でOption 94を個別固定（顧客指定IPv4を含む完全hex）。登録がないCEは接続できない（フォールバックなし）
     * subnet6（PD動的）— `/40`プール（`PD_POOL`）から`/56`ずつ動的委譲、Option 94はSubnet直下のflex-option式で付与
     * subnet6（PD固定）— `/56`はHost Reservationで固定払い出しせず、**PD動的と同一の`PD_POOL`**から都度動的に委譲する。`map-e-static-ip.conf`登録済みMACはHost Reservationの`option-data`でOption 94を個別固定（顧客指定IPv4＋`PD_FIX_POOL`由来の固定IPv6 Prefixを含む完全hex）。登録がないCEは接続できない（フォールバックなし）
     * hooks-libraries: `libdhcp_flex_option.so`（①②のSubnetにOption 94のflex-option式を`supersede`、[13章](#13-flex-option-詳細仕様)）＋ `libdhcp_run_script.so`（`kea-map-e-hook`、ルーティング注入・削除用）
     * reservations: ③④の`map-e-static-ip.conf`登録MACごとに、Option 94の完全固定hexを`option-data`として持つHost Reservationを生成（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）
     * ※複数VLANインターフェースを単一のPDプールに紐付けるため、Keaのshared-networks設定を使用する
   * `kea-map-e-hook` — DHCPv6リースイベントスクリプト（ルーティング注入・削除用）
     * `leases6_committed`: IA_PD確定時にIPv6ルート＋ip6tnlトンネル＋IPv4ルートを自動注入（IPv4は①②なら[5章](#5-ipv4アドレス算出ロジックflex-optionhost-reservation併用方式)の式、③④なら`map-e-static-ip.conf`のMAC引き当て結果をPerl/Pythonで再実装して算出。Kea側の結果と一致させる）
     * `lease6_release`/`lease6_expire`: リース解放・満了時に委譲プレフィックスのIPv6静的ルート（`ip -6 route del <prefix>`）を自動削除
     * MACアドレスからリンクローカルIPv6を逆引き（NDPキャッシュ→EUI-64フォールバック）

4. **DNS (BIND9)**（`./bind/`）
   * `named.conf` / `named.conf.local` — メイン設定およびゾーン定義
   * `db.map.ocn.ad.jp` — OCN MAP-Eゾーン（`@`, `rule`, `*` → BR IPv6アドレス）
   * `db.v6connect.net` — v6connectゾーン（`@`, `*` → BR IPv6アドレス）
   * `dynamic/db.<ドメイン>` — `ddns.conf` に記載されたドメインから自動生成される動的ゾーンファイル。RFC 2136（`nsupdate`）および FortiGate DDNS（`/nic/update`）によるホスト名・アドレスの自動登録をサポート
   * CEルーターがDNSを引いてprovisioning-serverのIPv6アドレスを解決する

5. **NTP (chrony)**（`./chrony/`）
   * `chrony.conf` — `map-e.conf` の `SYSTEM_NTP` からサーバーリスト生成、全IF/全ネットワークからのクライアント許可

6. **MAP-E 設定配布・ルーティング**（`./mape-provisioning-server/`）
   * `mape-provisioning-server` — HTTPSサーバー（Python3, Port 443）
     * CEからのGETリクエストに応答、JSON形式でMAP-Eルール（basicMapRules）を返却
     * 4つの認証パス: SLAAC（動的・固定）／DHCPv6-PD（動的・固定）
     * 送信元IPv6 → Keaリースファイル逆引き → MACアドレス特定
     * ①②（動的）：IPv4は[5章](#5-ipv4アドレス算出ロジックflex-optionhost-reservation併用方式)の式（`mape_calc`）で算出
     * ③④（固定）：MACをキーに`map-e-static-ip.conf`を検索し、登録済みの固定IPv4をそのまま採用（未登録の場合は 403 Forbidden で接続拒否）
     * ルーティング自動設定: ip6tnlトンネル作成＋IPv4ルート＋NDPキャッシュ静的登録
     * DDNS更新受付: FortiGate 等の HTTP DDNS クライアントからの `/nic/update` リクエストを受信し、DNS レコードを動的更新
     * テスト用アクセス手順: `curl` コマンド等によるテストアクセス手順の詳細については [付録C](#付録c-mape-provisioning-server-テストアクセス手順curl等) を参照
   * `mape_calc` — 共通計算ライブラリ（[5章](#5-ipv4アドレス算出ロジックflex-optionhost-reservation併用方式)のロジックをPerl/Pythonで再実装。Kea側flex-option式・Host Reservation個別指定と同一の結果になることをテストで担保する）
     * `calc_map_e_params()`: IPv6プレフィックス → IPv4＋CE用IPv6算出
     * `calc_ipv4_from_pd_prefix()`: 払い出された`/56`プレフィックスの委譲ID部分（バイトオフセット固定）を抜き出してIPv4を算出（PD動的）
     * `calc_ipv4_from_vlan_and_duid()`: VLAN番号＋DUIDの一部からIPv4を算出（SLAAC動的）
     * `lookup_static_ipv4(mac)`: `map-e-static-ip.conf`からMACに対応する固定IPv4を検索（③④、未ヒット時はNoneを返し接続不可）
     * `get_target_ipv4_and_dev_from_host_part()`: CE IPv6 → 埋め込みIPv4逆算
     * `get_tunnel_dev_name()`: IPv4 → トンネルデバイス名（`mpe-common`）
     * `convert_to_data_ipv6()`: プレフィックス＋IPv4 → データ通信用CE IPv6合成
     * `update_ddns()`: `nsupdate` を呼び出して BIND9 へ FQDN/IP レコードを動的登録
   * SSL証明書（自己署名）
     * CN/SAN: `*.ocn.ad.jp`（有効期限365日）

7. **固定IPv4マッピング**（`./map-e-static-ip.conf`）
   * フォーマット: `MACアドレス,固定IPv4/プレフィックス長`（カンマ区切り＋CIDR表記。`gen_kea_configs.pl` や `mape_calc` 側でカンマ分割および `/mask` 除去を行って固定IPv4を取得する。IPv6アドレスは変動するため記載不要。）
   * `kea-dhcp6.conf`のHost Reservation生成（`gen_kea_configs.pl`）が本ファイルを読み込み、MACごとにOption 94の固定hexを事前計算した`reservations`エントリを組み立てる（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）
   * `mape-provisioning-server`も同じファイルを参照し、Kea側と同一の固定IPv4をルーティング注入に用いる

8. **DDNS（Dynamic DNS）設定ファイル**（`./ddns.conf`）
   * フォーマット: `識別キー (MACアドレス または PPPoEユーザー名), IPv6用FQDN, IPv4用FQDN`
   * 1列目にクライアントのMACアドレス（12桁Hex/コロン区切り）またはPPPoEユーザー名を指定して個別FQDNを割り当て可能（未登録のCEは一律で標準形式 `ce-<IPv4アドレス>.map.ocn.ad.jp` が自動適用される）
   * `gen_bind_configs.pl` が本ファイルを読み込み、登場するドメイン名（例: `p-ns.flets-west.jp`）を自動抽出して BIND9 の動的ゾーンファイル（`bind/dynamic/db.<ドメイン>`）を自動生成し、DDNS 登録を可能にする
   * `mape_calc` (`update_ddns`) が CE の接続・アドレス確定時に `nsupdate` を呼び出し、個別登録のあるCEに対して IPv6用FQDNに AAAA レコード（`ce_ipv6`）、IPv4用FQDNに A レコード（`target_v4`）、および両ホストに **TXTレコード (`updated=YYYY-MM-DDTHH:MM:SS+09:00`)** を自動記録

9. **CEルーター設定例**（`./CE-setting`）
   * CEルーター（NEC IX, FortiGate, YAMAHA等）の設定例およびコマンド集（随時更新）
   * `tunnel mode map-e ocn-fixed` / `ocn-pd` によるMAP-Eトンネル構成
   * PPPoE 設定（マルチサービス、サービス名指定、アカウント設定等）

10. **PPPoE サーバー設定ファイル群**（`./pppoe/` および `./pppoe.conf`）
   * `pppoe.conf`: PPPoE アカウント一元定義ファイル（ユーザー名、パスワード、クライアント IP / 委任サブネット、サービス名、備考）
   * `pppoe/patches/pppoe-server-deny-list.patch`: 自動フェイルオーバー・1分間denyリスト機能パッチ
   * `pppoe/srv-<Service>/chap-secrets` / `pap-secrets`: 認証シークレットファイル
   * `pppoe/srv-<Service>/routes.conf`: 接続時に自動設定する委任プレフィックス・ルーティング定義
   * `pppoe/srv-<Service>/pppoe-server-options`: pppd 起動オプション（`ms-dns` による IPv4 BR アドレス通知を含む）
   * `pppoe/srv-<Service>/deny-mac.list`: 静的 MAC 拒否リスト

11. **Central NAT（外部接続 NAT）**（`./nat/`）
   * `nat/Dockerfile`: iptables 実行用コンテナイメージ
   * `nat/entrypoint.sh`: 非 VRF MAP-E / PPPoE パケットを外部へ MASQUERADE 送出する起動スクリプト（デフォルト無効: `NAT_ENABLED="0"`）

12. **Web 統合ダッシュボード**（`./web-dashboard/`）
   * `app.py`: 軽量 Python HTTP サーバー（Port 1600）
   * `static/index.html`: ダークモード・レスポンシブ UI（クライアント一覧、ルーティング、コンテナ状態）
   * `static/config.html`: 環境設定（Config Editor）画面
   * REST API: `/api/clients`（接続中 PPPoE / MAP-E クライアント一覧）、`/api/routes`（ルーティングテーブル）、`/api/services`（各コンテナ状態）、`/api/config`（設定取得・ステージング・反映）、`/api/logs`（ログ閲覧）、`/api/pppoe/disconnect`（PPPoE 切断）

---

## 12. map-e.conf パラメータ一覧

本節に記載のパラメータ値は設定例です。運用環境のネットワーク構成に応じて `map-e.conf` 上で自由に変更可能です。

### 12.1 管理系（共通）

| 変数名 | 設定値 | 説明 |
|---|---|---|
| `MGT_IF` | `eth0` | 管理用物理インターフェース |
| `MGT_IP` | `192.168.0.10/24` | 管理用IPv4アドレス / マスク長 (CIDR形式または個別指定) |
| `MGT_GW` | `192.168.0.1` | 管理用デフォルトゲートウェイ |
| `SYSTEM_DNS` | `192.168.0.1 1.1.1.1` | システム用DNS（フォワーダー） |
| `SYSTEM_NTP` | `192.168.0.1 ntp.example.jp` | システム用NTP |
| `DOMAIN` | `example.com` | ドメイン名 |
| `WEB_DASHBOARD_PORT` | `1600` | Web 統合ダッシュボードの待受ポート番号 |
| `NAT_ENABLED` | `0` | **外部接続 NAT (Central NAT) の有効化（1: 有効, 0: 無効、デフォルト: 0）**。非 VRF の MAP-E および PPPoE からのパケットを上位 GW 経由で外部 IPv4 へ NAT 接続 |

### 12.2 BR内部インターフェース・MAP-E共通

| 変数名 | 設定値 | 説明 |
|---|---|---|
| `MAPE_IF` | `eth1` | MAP-E物理・トランクインターフェース |
| `BR_IF` | `dummy0` | トンネル終端インターフェース（Linux dummy デバイス） |
| `PROV_IF` | `dummy1` | 共通サービス基盤インターフェース（Linux dummy デバイス） |
| `BASE_SUBNET` | `2001:db8` | 基幹IPv6プレフィックス |
| `BR_PREFIX` | `${BASE_SUBNET}:aaaa` | BRプレフィックス（96bit） |
| `BR_IPV4_ADDR` | `192.0.2.1` | BR IPv4（CEから見たGW、`BR_IPV6`合成、PPPoEクライアントへDNSとして通知、16進: `c000:0201`） |
| `BR_IPV6` | 自動生成（`BR_PREFIX` + `BR_IPV4_ADDR`の16進） | BRの完全IPv6アドレス（例: `2001:db8:aaaa::c000:0201`） |
| `BR_IPV4_POOL` | `198.51.0.0/16` | 全方式（SLAAC動的/PD動的）共有のIPv4プール。**アドレス衝突防止のため /16 固定**（/24 や /17 は使用不可）。Kea委譲IDやVLAN番号＋クライアントDUID末尾で`/32`を合成 |
| `MAPE_PROV_PREFIX` | `${BASE_SUBNET}:9999` | 共通サービス用プレフィックス |
| `MAPE_DNS_IP` | `${MAPE_PROV_PREFIX}::53` | 全方式共通のDNSアドレス |
| `MAPE_NTP_IP` | `${MAPE_PROV_PREFIX}::123` | 全方式共通のNTPアドレス |
| `MAPE_PROV_IP` | `${MAPE_PROV_PREFIX}::ff` | provisioning-serverアドレス |
| `MAPE_DOMAIN_SEARCH` | `map.example.ad.jp` | CEへ通知するドメインサーチリスト |

### 12.3 ① SLAAC動的

| 変数名 | 設定値 | 説明 |
|---|---|---|
| `SLAAC_DYN_VLANS` | `101 102` | このVLANはSLAAC動的として扱う（接続宣言例: **VLAN 101**） |
| `SLAAC_BR_BASE` | `1000` | IPv6プレフィックス4番目hextetの計算用ベース値（`${BASE_SUBNET}:${SLAAC_BR_BASE}+VLANID::/64`） |
| `SLAAC_BR_SUFFIX` | `::ff0a` | 各VLANのBR側アドレスのサフィックス |

### 12.4 ② DHCPv6-PD動的

| 変数名 | 設定値 | 説明 |
|---|---|---|
| `PD_DYN_VLANS` | `103 104` | このVLANはDHCPv6-PD動的として扱う（接続宣言例: **VLAN 103**） |
| `PD_POOL` | `2000` | 委譲プール（`${BASE_SUBNET}:${PD_POOL}::/40`、VLANを区別せず全VLAN共有） |

`/40`からの委譲単位（`/56`）はスクリプト内の固定値（変更不可）であり、`map-e.conf` に個別の変数は持たない。

### 12.5 ③ SLAAC固定

| 変数名 | 設定値 | 説明 |
|---|---|---|
| `SLAAC_FIX_VLANS` | `105` | このVLANはSLAAC固定として扱う（接続宣言例: **VLAN 105**） |
| `SLAAC_FIX_BR_PREFIX` | `3000` | IPv6プレフィックス4番目hextetの計算用ベース値（`${BASE_SUBNET}:${SLAAC_FIX_BR_PREFIX}+VLANID::/64`） |
| `SLAAC_FIX_BR_SUFFIX` | `::ff0d` | 各VLANのBR側アドレスのサフィックス |

### 12.6 ④ DHCPv6-PD固定

| 変数名 | 設定値 | 説明 |
|---|---|---|
| `PD_FIX_VLANS` | `106` | このVLANはDHCPv6-PD固定として扱う（接続宣言例: **VLAN 106**） |
| `PD_FIX_POOL` | `4000` | Option 94のRule IPv6 Prefixに埋め込む固定Prefix（`${BASE_SUBNET}:${PD_FIX_POOL}::/40`）。実際のIPv6 `/56`委譲には使用しない |

④のIPv6 `/56`はHost Reservationによる固定払い出しを行わず、**`PD_POOL`（②と同一のKea PDプールオブジェクト）から都度動的に採番する**（[14章](#14-アドレスプール設計とflex-optionの実現性検証)参照）。固定運用の対象はIPv4のみであり、`PD_FIX_POOL`はOption 94のRule IPv6 Prefix欄を埋めるための固定値としてのみ使われる点に注意。

### 12.7 map-e-static-ip.conf の例

MACアドレスと顧客指定の固定IPv4の対応表。カンマ区切りで `MACアドレス,固定IPv4/プレフィックス長` の形式で記述する（`gen_kea_configs.pl` や `mape_calc` スクリプト側でカンマ分割をパースし、`/mask` が付いている場合は自動でプレフィックス長を取り除いてIPv4を取得する）。IPv6（`/56`や`/64`）は記載しない（OCN商用でもIPv6アドレスは変動しうるため、固定運用の対象外とした）。③④共通で使用し、どちらのVLANに接続しても同じMACには同じIPv4が割り当てられる。

```text
# MACアドレス,固定IPv4/プレフィックス長 (設定例)
00:11:22:33:44:55,203.0.113.10/32
00:aa:bb:cc:dd:ee,203.0.113.20/32
```

`gen_kea_configs.pl`（[13章](#13-flex-option-詳細仕様)）が本ファイルを読み込み、MACごとにOption 94の完全な固定hex値（顧客指定IPv4を含む）を事前計算し、Kea Host Reservationの`option-data`として埋め込む（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）。③はVLAN（Subnet）ごとにIPv6部分が異なるためSubnet単位でreservationを複製し、④は共通固定値（PD_FIX_POOL）で統一されているためshared-network直下に1回だけ登録すればよい。

### 12.8 自動導出・派生変数一覧（MapeCommon.pm で自動算出）

`map-e.conf` 内で直接定義せず、スクリプト実行時（`MapeCommon::load_conf_file`）に他の設定値から自動的に補完・算出される派生変数一覧。

| 変数名 | 算出ロジック | 設定値 | 説明 |
|---|---|---|---|
| `BR_IPV6` | `BR_PREFIX` + `BR_IPV4_ADDR`(16進) | `2001:db8:aaaa::c000:0201` | BRの128bit完全IPv6アドレス |
| `MAPE_FLAT_RULE_IPV6_PREFIX` | `${BASE_SUBNET}::` | `2001:db8::` | MAP-E Rule IPv6 Prefix (Flat Mode用) |
| `MAPE_FLAT_RULE_IPV6_LEN` | `32` (BASE_SUBNETヘクステット数×16) | `32` | MAP-E Rule IPv6 Prefix Length |
| `MAPE_PD_PREFIX` | `${BASE_SUBNET}:${PD_POOL}:` | `2001:db8:2000:` | DHCPv6-PD動的配布用プレフィックス |
| `MAPE_PD_FIX_PREFIX` | `${BASE_SUBNET}:${PD_FIX_POOL}:` | `2001:db8:4000:` | DHCPv6-PD固定用プレフィックス |

### 12.9 PPPoE 関連パラメータ

PPPoE サーバーの動作およびマルチサービス分離・自動フェイルオーバーを制御するパラメータ群。

| 変数名 | 設定値 | 説明 |
|---|---|---|
| `PPPOE_VLANS` | `107` | PPPoEサーバーを収容するVLAN ID一覧（空白区切り。MAP-E用の各VLANはmake generate時に自動マージされる） |
| `PPPOE_SERVER_BASE_IP` | `203.0.113.0` | サーバー対向IP基底アドレス（未指定時等のベース） |
| `PPPOE_SERVICES` | `Service_A Service_B Internet` | 起動する全サービス名一覧（空白区切り） |
| `PPPOE_SERVER_IP_<Service>` | `203.0.113.1` 等 | 各サービスの固定対向サーバーIP（例: Service_Aは `203.0.113.1`、Service_Bは `203.0.113.2`、Internetは `203.0.113.3`） |
| `PPPOE_VRF_ENABLED_<Service>` | `1` または `0` | 各サービスの VRF 分離設定（1: VRFによる閉域網分離を有効化, 0: 非VRF・外部接続NAT対象） |
| `PPPOE_SERVER_VLANS_<Service>` | `101 107` 等 | 各サービスを収容する VLAN ID 一覧（未指定時は全アクティブ VLAN にバインド） |

※ **MAP-E VLAN の自動マージ機能**: `gen_pppoe_configs.pl` の実行時、`SLAAC_DYN_VLANS`, `PD_DYN_VLANS`, `SLAAC_FIX_VLANS`, `PD_FIX_VLANS` に含まれる全 VLAN が自動的に PPPoE サーバーの生成対象へ追加されます。これにより、`PPPOE_VLANS` は PPPoE 専用 VLAN（例: `107` 等）のみを記述しておけば、すべての検証用 VLAN で PPPoE サービスが利用可能となります。

---

## 13. flex-option 詳細仕様

`kea-dhcp6` に `libdhcp_flex_option.so` をロードし、**RFC 7598 準拠 Option 94（OPTION_S46_CONT_MAPE）** の値を、動的接続方式（①②）についてはSubnet／Client Class単位の式（expression）で組み立てる。固定接続方式（③④）はflex-optionを使わず、Host Reservationごとの`option-data`に事前計算済みの完全な固定hexを直接設定する（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）。Kea設定ファイル自体がSubnet／Class／Reservationごとに静的な値・式を持つ。

### 13.1 hooks-libraries設定の全体構造

```jsonc
{
  "Dhcp6": {
    "hooks-libraries": [
      { "library": "libdhcp_flex_option.so",
        "parameters": {
          "options": [
            {
              "code": 94,
              "supersede": "<Subnet固有の式。①②のSubnetに設定。13.2節/13.3節参照>",
              "csv-format": false,
              "client-class": "<VLAN/接続方式を表すクラス名>"
            }
          ]
        }
      },
      { "library": "libdhcp_run_script.so",
        "parameters": { "name": "/usr/local/bin/kea-map-e-hook", ... }
      }
    ]
  }
}
```

`libdhcp_flex_option.so`の式は**クライアントがそのリクエストパケットで送ってきた内容（クエリパケット）にのみアクセスできる**。`kea-dhcp6.conf.tmpl`の生成時（ [gen_kea_configs.pl](generator/gen_kea_configs.pl) ）に、VLANごとの固定部分（BR IPv6、IPv4上位バイト、prefix6-len等）をあらかじめ置換処理で埋め込み、式自体はSubnetごとに完全に静的な文字列として出力する。

なお、`gen_kea_configs.pl` が生成する Option 94（Container Option: `OPTION_S46_CONT_MAPE`）内には、Option 90（BR Option: `0x005a...`）および Option 89（Rule Option: `0x0059...`）に加え、動的セグメントでは末尾に Option 93（Port Parameters Option: `OPTION_S46_PORTPARAMS`: code 93, `0x005d...`、offset-len 4bit + psid-len 8bit + PSID 16bit）が結合されて送出される。

### 13.2 PD動的（②）の flex-option 式

`/16` プール設計（[14章](#14-アドレスプール設計とflex-optionの実現性検証)）に基づき、下位16bitを委譲ID可変部（1byte）とDUID末尾（1byte）から合成する。

```
supersede:
  concat(
    <BR_IPV6の16byte hex 固定値>,                 // OPTION_S46_BR (type90, len16)
    concat(
      <flags(0x00)+ea-len(0x08)+prefix4-len(0x20) 固定3byte>,  // OPTION_S46_RULE ヘッダ
      concat(
        <BR_IPV4_POOL上位2byte 固定hex>,
        concat(
          concat(
            substring(option[25].option[26].hex, 15, 1), // /56委譲プレフィックスのoffset 15 (1byte)
            substring(option[1].hex, -1, 1)              // DUID末尾 (1byte)
          ),
          concat(
            <prefix6-len(0x38=56)+IPv6prefix7byte 固定/Subnet別hex>,
            concat(
              <OPTION_S46_PORTPARAMS ヘッダ (0x005d0004040800) 7byte>, // Option 93 (offset-len 4, psid-len 8)
              substring(option[25].option[26].hex, 15, 1)               // target_psid (1byte)
            )
          )
        )
      )
    )
  )
```

この式は②（PD動的）のSubnetに設定する。なお、④（PD固定）はHost Reservation登録済みMACにのみOption 94を配布するため本式は適用されず、登録がないCEは接続できない。

### 13.3 SLAAC動的（①）の flex-option 式

SLAACにはKeaによるアドレス払い出し（IA_NA）が存在しないため、VLAN番号（Subnet生成時に固定値として埋め込み）とクライアントDUID（`option[1].hex`、Client Identifier）の一部を組み合わせる。

```
supersede:
  concat(
    <BR_IPV6の16byte hex 固定値>,
    concat(
      <flags(0x00)+ea-len(0x08)+prefix4-len(0x20) 固定3byte>,
      concat(
        <BR_IPV4_POOL上位2byte 固定hex>,
        concat(
          concat(<このVLAN番号を表す1byte固定hex>, substring(option[1].hex, -1, 1)),  // 下位1byteはDUID末尾1byte
          concat(
            <prefix6-len(0x40=64)+IPv6prefix8byte 固定/Subnet別hex>,
            <OPTION_S46_PORTPARAMS(type93, offset-len=4, psid-len=8, PSID) (0x005d0004040800 + psid_hex)>
          )
        )
      )
    )
  )
```

この式は①（SLAAC動的）のSubnetに設定する。なお、③（SLAAC固定）はHost Reservation登録済みMACにのみOption 94を配布するため本式は適用されず、登録がないCEは接続できない。

### 13.4 SLAAC固定（③）・PD固定（④）：登録済みMACのOption 94個別指定

③④で`map-e-static-ip.conf`に登録されたMACは、Kea Host Reservationの`option-data`としてOption 94の完全な固定hexが直接設定される（式評価は行わない）。

```jsonc
{
  "hw-address": "<登録MAC>",
  "option-data": [
    {
      "code": 94,
      "csv-format": false,
      "data": "<下記の内訳で事前計算した完全固定hex文字列>"
    }
  ]
}
```

hexの内訳（`gen_kea_configs.pl`が`map-e-static-ip.conf`とVLANごとの固定値から計算）：

```
data:
  concat(
    <BR_IPV6の16byte hex 固定値>,                          // OPTION_S46_BR
    concat(
      <flags(0x00)+ea-len(0x00)+prefix4-len(0x20) 固定3byte>,  // OPTION_S46_RULE ヘッダ
      concat(
        <map-e-static-ip.confの固定IPv4を16進変換した4byte>,     // 顧客指定IPv4
        <prefix6-len+IPv6prefix 固定hex>                         // ③:実VLANプレフィックス／④:PD_FIX_POOL固定値
      )
    )
  )
```

### 13.5 ea-len・prefix4-len等の固定値

| 接続方式 | flags | ea-len | prefix4-len | prefix6-len | Option 94の生成方式 |
|---|---|---|---|---|---|
| ①SLAAC動的 | `0x00` | `0x08` | `0x10`(16) | `0x40`(64) | flex-option supersede (静的hex) |
| ②PD動的 | `0x00` | `0x08` | `0x10`(16) | `0x30`(48) | flex-option supersede (静的hex) |
| ③SLAAC固定・登録済み | `0x00` | `0x00` | `0x20`(32) または `0x1c`(28) | `0x38`(56) | Host Reservation個別`option-data`（完全固定hex） |
| ③SLAAC固定・未登録 | - | - | - | - | 接続不可（Option 94 未配布・拒否） |
| ④PD固定・登録済み | `0x00` | `0x00` | `0x20`(32) または `0x1c`(28) | `0x28`(40) | Host Reservation個別`option-data`（完全固定hex、Rule IPv6 PrefixはPD_FIX_POOL） |
| ④PD固定・未登録 | - | - | - | - | 接続不可（Option 94 未配布・拒否） |

---

## 14. アドレスプール設計とflex-optionの実現性検証

本章はアドレスプール設計案がflex-optionで実現可能かどうかの検証結果をまとめたものである。

### 14.1 flex-option式言語の制約

Kea公式ドキュメント（Client Classification / Flexible Option Hooks Library）を確認した結果、式言語で利用できる関数は次のみである。

* 文字列操作：`substring(value, start, length)`、`concat(a, b)`、`split(value, delimiters, field)`、`ifelse(cond, a, b)`、`hexstring(value, sep)`
* 変換：`int8totext`/`int16totext`/`int32totext`/`uint8totext`/`uint16totext`/`uint32totext`
* 存在判定・比較：`option[x].exists`、`==`など

**加算・減算・剰余（mod）・ビットシフト・ビットマスクといった算術演算は一切提供されない。** つまりflex-optionでできるのは「クエリパケット中の既存バイト列を、そのまま切り出して並べ替える」ことだけであり、「任意サイズのプールへ値を分散させる」処理は表現できない。

### 14.2 結論：/16 プールのみが安全に実現可能

* **/16 プール（host部16bit＝2byte）**：委譲プレフィックスの可変バイト（1byte）とクライアントDUID（1byte）をそのまま `concat` して 2byte を構成することで、バイト境界に完全に一致して過不足なく安全に `/32` を合成できる。**実現可能。**
* **/24 または /17 プール**：8の倍数bitでない、あるいは1byteしか切り出せない場合、Kea式言語ではビットシフトやビットマスクができないため、アドレス重複やプールの境界超過が発生する。したがって、**本環境では `BR_IPV4_POOL` は `/16` 固定**とする。

### 14.3 固定接続方式のIPv6委譲と接続制御

③④の**固定IPv4自体**はHost Reservation個別`option-data`で顧客指定値をそのまま設定するため、flex-option式のバイト抽出ロジックには依存しない（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）。

一方、④(PD固定)のIPv6 `/56`委譲についてはHost Reservationでの固定払い出しを行わず、**②(PD動的)と同一のKea PDプールオブジェクト（`PD_POOL`）から都度動的に採番する**設計とした。なお、登録がないCEが固定IPのVLANに接続した場合は接続できない（フォールバック動作は行わない）。

---

## 15. PPPoE エミュレーション環境設計仕様

### 15.1 概要・マルチサービス分離

同一の物理/VLANインターフェース（例: `<MAPE_IF>.<VLAN_ID>` / `eth1.107` 等）上で、複数の独立した PPPoE サービス（例: `Service_A` / `Service_B` / `Internet`）を Docker コンテナとして同時稼働させる。
これにより、センター機器やCEルーターからマルチセッション接続（PPPoE-1, PPPoE-2 等）を行った際に対向サーバー IP（ゲートウェイ）が重複せず、明瞭に分離された試験環境を提供する。

* **サービス分離および VRF 閉域化の実現方法**:
  * RFC 2516（PPPoE仕様）に準拠し、**統合型 PPPoE サーバー（`pppoe-server-unified`）** が単一コンテナ内で全 VLAN・全サービスを収容する。
  * 全ユーザーの認証情報を保持し、ユーザー名から動的に VRF を振り分ける。`user-services.conf`（接続時に動的生成）がユーザー名 → サービス名・VRF設定のマッピングを提供する。
  * `pppoe-server` 起動時に `-C <サービス名>` および `-S <サービス名>` を各サービスごとに指定することで、クライアントはサービス名でフィルタリングして接続可能。
  * **VRF 閉域化制御 (`PPPOE_VRF_ENABLED_<Service>`)** (例):
    * `Service_A` (VRF: `1`): ホスト上に専用の Linux VRF デバイス（例: `vrf-Service_A`）が自動生成され、該当 ppp インターフェースが VRF 内にバインドされる。これにより独立したルーティング空間が形成され、外部や他サービスから完全に遮断される。
    * `Internet` (VRF: `0`): メインルーティングテーブルに配置され、`NAT_ENABLED="1"` 設定時に外部ネットワークへ透過的に NAT 送出される。
  * クライアント側でサービス名（例: RTX の `pppoe service name Service_A`、FortiGate の service-name）を指定して接続することで、目的のサービスへ直接接続可能。
  * **DNS アドレス通知（IPCP ネゴシエーション）**: `pppoe-server-options` に `ms-dns <BR_IPV4_ADDR>` を設定し、PPPoE クライアント接続時に IPv4 BR アドレスを DNS サーバーとして自動通知する。
  * **VRF 閉域内からの DNS サーバーアクセス**:
    * VRF 閉域（`unreachable default`）環境下のクライアントからでも通知された DNS（`BR_IPV4_ADDR`）への名前解決を可能にするため、以下の連携機構を装備する。
    * **VRF テーブルへの local ルート自動登録**: `system/vrf-setup.sh` 生成時および接続時 `pppoe/ip-up` において、VRF テーブルに `local <BR_IPV4_ADDR> dev lo` を自動登録し、VRF 内からの DNS 宛てパケットをローカル着信させる。
    * **VRF 間ソケット受入許可（sysctl）**: `net.ipv4.udp_l3mdev_accept = 1` および `net.ipv4.tcp_l3mdev_accept = 1` を有効化し、ホスト上のデフォルト VRF で動作する BIND9（`0.0.0.0:53`）が VRF からの UDP/TCP クエリを安全に受信する。
    * **DNS 応答パケットのポリシーベースルーティング（`ip rule`）**: `pppoe/ip-up` 時に `ip rule add from <BR_IPV4_ADDR> to <REMOTE_IP>/32 table <TABLE_ID>`（委譲サブネット時は `to <subnet>` も同様）を動的に追加。送信元が DNS アドレスに厳格に限定されているため、閉域網のセキュリティ・隔離性を 100% 保持したまま、DNS 応答パケットのみを正確に VRF の CE へ返送する。切断時（`pppoe/ip-down`）には該当ルールを自動削除してクリーンアップする。
  * **【重要】サービス名の命名規則と自動変換**:
    * `map-e.conf` の `PPPOE_SERVICES` や `pppoe.conf` の第4カラムはスペース区切りフォーマットであり、シェル変数名や Docker Compose サービス名にも用いられるため、サービス名自体にスペースを含めることはできない。
    * そのため、Web UI や設定反映処理において、サービス名に含まれるスペース（空白）はすべて **`_`（アンダースコア）に自動変換・正規化** される（例: `Service A` → `Service_A`）。
    * **サービス名の最大長は 11 文字**。VRF デバイス名 `vrf-<サービス名>` が Linux インターフェース名の 15 文字制限に収まるよう、11 文字以上のサービス名はバリデーションエラーとなる。

### 15.2 アカウント定義ファイル（pppoe.conf）とディスパッチ規則

全 VLAN・全サービスの PPPoE アカウント設定は、単一の共通ファイル `pppoe.conf` に一元記述する。

```text
# Username         Password Client_IP_or_Subnet Service_Name Note (設定例)
user01@service_a   pass01   203.0.113.10/24     Service_A    Client_01_Service_A
user02@service_b   pass02   203.0.113.20/29     Service_B    Client_02_Service_B
user03@internet    pass03   203.0.113.30/28     Internet     Client_03_Internet
common@example     pass99   203.0.113.99/32     *            全サービス共通アカウント
```

* **Web UI による CSV 直接編集モード**:
  * Web ダッシュボード（`web-dashboard`）の PPPoE ユーザー設定では、1行ずつのセル入力に加え、**「📝 CSV編集モード」** ボタンにより広いテキストエリアで一括入力・編集が可能。
  * カンマ区切り（CSV）、タブ区切り（TSV / Excelからの貼り付け）、半角スペース区切りのいずれも自動判別してパースされ、テーブル表示モードとの双方向同期に対応する。
* **第4カラムによる振り分けロジック**:
  * `*`: すべてのサービスコンテナの認証ファイル（`chap-secrets` / `pap-secrets`）および `routes.conf` に登録。
  * 特定のサービス名（例: `Service_A` または `Internet`）: 一致するサービスコンテナの設定にのみ登録。
  * 未指定時: デフォルトで `*`（全サービス共通）として扱う。
* **クライアントIPおよび委任サブネット**:
  * `203.0.113.10/32`: クライアント自身に `/32` を割り当て。
  * `203.0.113.10/24`: 委任サブネットを指定可能。クライアントに、その IP アドレスと、サブネットを割り当てる。
  * サブネットの範囲は /24 ～ /32
* **MAP-E VLAN との自動マージとクリーンアップ**:
  * `make generate` 実行時、`gen_pppoe_configs.pl` が `PPPOE_VLANS` に加えて MAP-E の全 VLAN（例: `101〜106`）を自動検知して合算し、PPPoE サービスディレクトリおよび `docker-compose.yml` を生成する。
  * 設定画面や `map-e.conf` から特定のサービス定義が削除された場合、`ConfigManager` が `pppoe/` 配下の不要になった旧ディレクトリ（`pppoe/srv-<Service>`）を自動的に検知して完全クリーンアップする。

### 15.3 統合型 PPPoE サーバー（一元収容方式）の構成

本環境では、全 VLAN・全サービスを単一のコンテナ（`pppoe-server`）で一元収容する統合型構成を採用している。

* **一元収容と応答制御**:
  * 単一サーバーが全 VLAN 上のブロードキャスト要求（PADI）を受信・応答するため、複数コンテナ構成のようなサーバー間の応答競合やフェイルオーバー待機時間（MAC 拒否タイマー等）は一切発生せず、極めて高速かつ決定論的にセッションが確立される。
* **動的 VRF 振分けとポリシー制御**:
  * クライアント接続時、`pppoe/ip-up` が `user-services.conf` を参照して認証ユーザーごとに所属サービスおよび VRF 有効/無効を判定。
  * VRF 閉域サービスに属するセッションは専用 VRF デバイス（例: `vrf-Service_A`）へ動的にアタッチされ、完全な閉域分離が維持される。
* **軽量・高信頼な標準構成**:
  * Debian 公式の標準パッケージ（`rp-pppoe` / `ppp`）のみで構成されており、カスタムパッチや外部依存を排除した高信頼・保守性の高い設計となっている。

---

## 16. Web 統合ダッシュボード仕様

### 16.1 概要・構成
本エミュレーション環境の稼働状態をブラウザから一元監視・設定できる Web ダッシュボードを **Docker コンテナ**（`map-e/web-dashboard`）として提供する。Port 1600 でアクセスでき、監視機能に加え、環境設定（`map-e.conf` 等）を **WebUI から直接編集・反映**することができる。

* **アクセス URL**: `http://<DebianホストIP>:1600/` （独立設定画面: `/config`）
* **アーキテクチャ**:
  * バックエンド: Python 3 組み込み `http.server`（外部依存ライブラリ不要）、モジュール分割構成：
    * `constants.py` — グローバル定数・環境変数解決
    * `config_app.py` — `AppConfig`（`map-e.conf` の読み込み・動的パラメータ提供）
    * `config_manager.py` — `ConfigManager`（設定ファイル群の読み書き・ステージング・反映）
    * `config.py` — アダプターモジュール
    * `utils.py` — `run_cmd` / `ping_target` / `KeaControlClient` / ログ処理ヘルパー群
    * `collectors.py` — `PPPoECollector` / `MAPECollector`（データ収集・整形・死活監視）
    * `handlers.py` — `DashboardHandler`（全 API エンドポイント・静的ファイル配信）
    * `app.py` — エントリーポイント（HTTPサーバー起動のみ）
  * フロントエンド: Vanilla JS + HTML5 + CSS（ダークモード/ライトモード/Bearded Theme対応）
    * `static/index.html` — メインダッシュボード HTML 構造
    * `static/config.html` — 環境設定（Config Editor）画面
    * `static/css/main.css` — スタイルシート
    * `static/js/main.js` — 初期化・定期ポーリング・メトリクス更新
    * `static/js/tables.js` — クライアントテーブル描画・ソート・XSSサニタイズ
    * `static/js/modals.js` — クライアント詳細・切断確認モーダル制御
    * `static/js/config-editor.js` — 環境設定エディタのフォーム制御・バリデーション
    * `static/js/theme-manager.js` / `bearded-themes.js` — カラーテーマ管理（全41テーマ）
> [!NOTE]
> **Bearded Theme 定義の生成・更新について**
> Web ダッシュボードで使用されるカラーテーマ定義（`web-dashboard/static/js/bearded-themes.js`）は既に生成済みで同梱されているため、通常運用時に外部リポジトリは不要です。テーマ定義を再生成・カスタマイズする場合のみ、リポジトリルートにて `git clone https://github.com/BeardedBear/bearded-theme.git` を実行してクローンした上で、`perl generator/gen_bearded_themes.pl` を実行してください。（`bearded-theme/` ディレクトリ自体は本リポジトリの管理対象外です）
  * ネットワーク: `network_mode: host`（ホストのルーティングテーブル、インターフェース、syslog ログを直接参照）
  * コンテナ実行ユーザー: UID/GID `1000:1000`（非 root）

### 16.2 主な機能と表示項目

1. **PPPoE クライアントテーブル**:
   * **ステータス**: Active バッジ ＋ 並行 Ping による RTT 応答時間バッジ（`● 0.2 ms` / `✕ 応答なし`）
   * **アカウント**: ユーザー名（`user01@example` 等）
   * **クライアント IP**: CIDR表記。`/32` の場合は省略して `203.0.113.10` のように表示。サブネット委任の場合は `203.0.113.0/29` のように表示
   * **サービス名バッジ**: 衝突防止アルゴリズムによる高コントラストカラーバッジ。VRF 閉域サービスには 🔒 アイコンを表示
   * **備考**: `pppoe.conf` の備考欄。割当 IP による多層逆引きにより欠落なく確実に表示
   * **接続時刻・接続時間**: 接続開始日時および経過時間（Uptime: 例 `4h 20m`）
   * **切断アクション**: 「切断」ボタン押下により、REST API（`POST /api/pppoe/disconnect`）経由で該当 `pppd` プロセスへ `SIGTERM` を送信して即時切断
   * **詳細モーダル**: 行クリックで PID、MAC、対向サーバー IP、PPPoE セッション ID 等の全パラメータをモーダル表示
2. **MAP-E (IPoE) クライアントテーブル**:
   * **ステータス**: Active / Expired バッジ ＋ 並行 Ping による RTT 応答時間バッジ
   * **VLAN バッジ**: 横幅を抑えた 2行コンパクトバッジ（`VLAN` / `<VLAN_ID>`）
   * **接続方式**: 方式スタックバッジ（SLAAC / DHCP-PD、動的 / 固定）
   * **委譲プレフィックス**: `/56` プレフィックス（DHCP-PD時）または `/64`
   * **払い出し IPv4**: `mape_calc` で算出した個別割り当て IPv4（`/32`）。CE が EA-bits から導出し、BR 側がトンネルルートとして登録した実アドレスを表示
   * **CE 側 IPv6 アドレス**: トンネル対向 IPv6 アドレス
   * **備考**: `map-e-static-ip.conf` 登録内容の表示
   * **リース期間**: リース開始時刻および満了時刻
   * **切断アクション**: DHCP-PD クライアントは Kea コントロールソケット経由でリースを即時解放
3. **ルーティングテーブル表示**:
   * **IPv4 ルーティング**: 宛先ネットワーク、ゲートウェイ、インターフェース
   * **IPv6 ルーティング**: リンクローカル自動生成行（`fe80::/64`）を `fe80::/64 (省略)` として 1行に自動集約
4. **システム稼働ステータス（コンテナ稼働状況カード）**:
   * システムを構成する各 Docker コンテナ（`radvd`, `kea-dhcp6`, `provisioning-server`, `pppoe-server`, `bind9`, `axosyslog`, `chrony` 等）の稼働状態・ヘルスチェック結果をリアルタイム表示。
5. **リアルタイムログビューア（Log Viewer）**:
   * `PPPoE`, `MAP-E`, `Kea DHCPv6`, `BIND9`, `radvd`, `Chrony`, `System` の各ログをリアルタイム閲覧可能。
   * ログレベル絞り込み（INFO以上 / 全ログ / WARN以上 / ERROR以上）およびキーワード検索フィルタを装備。
6. **設定変更（Config Editor）**:
   * **外部接続 NAT (Central NAT)**: Tab 1（VLAN 設定）の最上部にトグルカードとして配置。即座に NAT の有効/無効を切り替え可能（デフォルト: 無効）。
   * **VLAN 設定**: MAP-E VLAN（SLAAC動的/固定、PD動的/固定）と PPPoE 専用 VLAN を分離して管理。MAP-E の VLAN 削除時に PPPoE 側に残留しない安全設計。
   * **PPPoE 設定**: サービスインスタンス定義（サーバーローカル IP、VRF 閉域トグル、収容VLAN）および共通アカウント定義（CSV 一括編集モード対応）。
   * **ステージング＆反映**: 編集内容は `.config_staging.json` に一時保存され、「🚀 設定を反映する」で `make generate` → `docker compose up -d` が自動実行される。
   * **設定初期化** ボタン（🔄）: テンプレートファイルから初期値を復元（管理 LAN 設定は維持）。
   * **コンテナ再起動** ボタン（⟳）: 設定ファイルを書き換えずに、ネットワーク設定の再適用と全コンテナの再起動を実行。

---

## 付録A. CEルーター設定リファレンス

IX ルーターの設定は、ひかり電話の有無により2パターンを使用する。

**動的IPv4アドレスの場合**
- <https://jpn.nec.com/univerge/ix/Support/ipv6/OCN-VC/index.html#ipv6ra>
- <https://jpn.nec.com/univerge/ix/Support/ipv6/OCN-VC/index.html#dhcpv6pd>

**固定IPv4 1アドレスの場合**
- <https://jpn.nec.com/univerge/ix/Support/ipv6/OCN-VC/static-ip1.html#ipv6ra>
- <https://jpn.nec.com/univerge/ix/Support/ipv6/OCN-VC/static-ip1.html#dhcpv6pd>

**固定IPv4複数アドレスの場合**
- <https://jpn.nec.com/univerge/ix/Support/ipv6/OCN-VC/static-ip8.html#ipv6ra>
- <https://jpn.nec.com/univerge/ix/Support/ipv6/OCN-VC/static-ip8.html#dhcpv6pd>

**DHCPv6クライアントプロファイル**（NTP・ドメインサーチリストを追加）
```text
ipv6 dhcp client-profile dhcpv6-cl
  option-request dns-servers
  option-request domain-search-list
  option-request ntp-servers
```

YAMAHA RTX の設定例
https://www.rtpro.yamaha.co.jp/RT/docs/ocn_map_e/index.html#setting1

Fortigate の設定例
https://mytech-blog.com/fortigate-ipoe/#index_id7

---

## 付録B. ログ方針

ログは、出力できない特別な事情がない限り 514/tcp, 514/udp の syslog へ出力する。

---

## 付録C. mape-provisioning-server テストアクセス手順（curl等）

`mape-provisioning-server`（HTTPS / Port 443）の動作確認やレスポンス検証を、CEルーターの実機を使わずに `curl` コマンド等でテストアクセスする手順を以下に示す。

### C.1 前提要件と注意点

* **HTTPS / 自己署名証明書**: サーバーは自己署名証明書（`server.crt`）で動作しているため、`curl` の `-k`（`--insecure`）オプションが必要。
* **IPv6 アドレス表記**: `curl` で IPv6 生アドレスを指定する場合は `-g`（`--globoff`）オプションを付与し、`[<MAPE_PROV_IP>]` のように角括弧で囲む（例: `[2400:4150:99::ff]`）。
* **プロビジョニングサーバーアドレス**: `${MAPE_PROV_IP}`（現行値: `2400:4150:99::ff`、`map-e.conf` で定義）。BINDが動作している場合はドメイン名 `map.ocn.ad.jp` や `v6connect.net` でもアクセス可能。
* **自動ルーティング注入の発生**: テストアクセスを行うと、サーバーは指定された接続元IPv6または `ipv6Prefix` に応じたIPv4ルート（`ip route add <IPv4>/32 dev mpe-common encap ip6...`）をBRのカーネルテーブルへ自動設定する。

### C.2 テストアクセス用 curl コマンド例

#### 1. 基本疎通・動作確認 (SLAAC動的/固定)
SLAAC環境（①③）ではCEがグローバルIPv6アドレスからリクエストを送信するため、指定インターフェース経由またはグローバルIPv6送信元アドレスを指定してアクセスする。

```bash
# 共通サービスアドレス ([<MAPE_PROV_IP>]) へ直接アクセス (例: 2400:4150:99::ff)
curl -k -g -6 "https://[2400:4150:99::ff]/"

# ドメイン名指定でアクセス (DNS解決確認兼)
curl -k -g -6 "https://map.ocn.ad.jp/"

# 特定のVLANインターフェース (例: ens19.60) を送信元として指定
curl -k -g -6 --interface ens19.60 "https://[2400:4150:99::ff]/?ipv6Prefix=::&ipv6PrefixLength=0"
```

#### 2. DHCPv6-PD環境のエミュレーション (ipv6Prefix クエリパラメータ付与)
DHCPv6-PD環境（②④）では、CEとBR間の対向通信がリンクローカルアドレス（`fe80::`）で行われるため、CEは委譲された `/56` プレフィックスを `ipv6Prefix` クエリパラメータでサーバーに通知する。

```bash
# 委譲プレフィックス (例: 2400:4150:2000:100:: 等) を指定してアクセス
curl -k -g -6 "https://[2400:4150:99::ff]/?ipv6Prefix=2400:4150:2000:100::&ipv6PrefixLength=56"
```

#### 3. 応答JSONフォーマットの確認 (jq 連携)
`curl` に `-s`（silent）と `jq` を組み合わせることで、整形されたJSONレスポンスを確認できる。

```bash
curl -sk -g -6 "https://[2400:4150:99::ff]/?ipv6Prefix=2400:4150:2000:100::" | jq .
```

**正常応答例 1: 動的接続 (SLAAC動的 / PD動的)**:
```json
{
  "basicMapRules": [
    {
      "ipv6Prefix": "2400:4150:2000::",
      "ipv6PrefixLength": "40",
      "ipv4Prefix": "10.218.0.0",
      "ipv4PrefixLength": "16",
      "brIpv6Address": "2400:4150:3620::647f:ffff",
      "eaBitLength": "16",
      "psIdOffset": "0",
      "hostName": "ce-10.218.0.1.map.ocn.ad.jp",
      "psId": "0"
    }
  ]
}
```

**正常応答例 2: 固定IP接続 (PD固定 / SLAAC固定 - 商用 OCN バーチャルコネクト仕様)**:
```json
{
  "basicMapRules": [
    {
      "ipv6Prefix": "2400:4150:4000::",
      "ipv6PrefixLength": "40",
      "ipv4Prefix": "172.16.19.32",
      "ipv4PrefixLength": "28",
      "brIpv6Address": "2400:4150:3620::647f:ffff",
      "eaBitLength": "0",
      "psIdOffset": "0",
      "hostName": "ce-172.16.19.33.map.ocn.ad.jp"
    }
  ]
}
```

#### 4. FortiGate DDNS更新エンドポイントへのテストアクセス (`/nic/update`)
FortiGate 等の DDNS クライアントからの動的登録リクエストをシミュレートする。

```bash
curl -k "https://[2400:4150:99::ff]/nic/update?hostname=ce-test.map.ocn.ad.jp&myip=10.218.0.1"
```
成功時には `good 10.218.0.1` または `nochg 10.218.0.1` が返却され、BIND9 の動的ゾーンに A / AAAA / TXT レコードが自動登録される。

### C.3 実行結果・ログおよびルーティングの検証

テストアクセス実行後、以下のコマンドでサーバーログおよびBR側自動ルーティングの反映状態を確認する。

1. **サーバー側ログの確認** (`syslog` / `docker logs`):
   ```bash
   docker logs mape-provisioning-server --tail 20
   ```
   *ログ出力例*:
   `Request received - URL: /?ipv6Prefix=2400:4150:2000:100:: from Client-IP: fe80::...`
   `Payload to Router: {"basicMapRules": [...]}`

2. **自動注入ルートの確認** (`ip route`):
   ```bash
   ip route show dev mpe-common
   ```
   *出力例*:
   `10.218.0.1 dev mpe-common encap ip6 src 2400:4150:3620::647f:ffff dst 2400:4150:2000:100:a:da00:100:0`

---

## 付録D. 環境デバッグ用に用意しておくとよいツール・コマンド集

> [!IMPORTANT]
> **【重要】通常運用時は不要**
> 本セクションに記載されている各種ツール、パッケージ、およびコマンドは、**通信不具合の切り分け、CEルーター接続時のパケット検証、プロトコル挙動の調査等を行うデバッグ・トラブルシューティング作業時のみ一時的に用意・利用するもの**です。
> 通常のISPエミュレーション運用時（安定稼働時・定常試験時）にはこれらは不要であり、システムリソース保護やセキュリティの観点からも、常時インストール・常駐させる必要はありません。

### D.1 パケットキャプチャ・L2/L3ネットワーク解析ツール

1. **`tcpdump`** (パッケージ: `tcpdump`)
   - **用途**: 物理インターフェース（`<MAPE_IF>` / 例: `eth1` 等）、VLANインターフェース（`eth1.101`, `eth1.107` 等）、MAP-Eトンネル（`mpe-common`）、PPP仮想インターフェース（`ppp*`）を通過する生パケットのリアルタイムキャプチャ。
   - **典型的なデバッグコマンド例**:
     - *VLANタグ付きパケットの監視（各接続方式のVLAN分離確認）*:
       ```bash
       # 例: VLAN 101, 103, 105, 106, 107 等を指定してキャプチャ
       tcpdump -i eth1 -e -n -v "vlan 101 or vlan 107"
       ```
     - *DHCPv6 (SOLICIT, ADVERTISE, REQUEST, REPLY) の送受信確認*:
       ```bash
       tcpdump -i any -n -vv "ip6 and (udp port 546 or udp port 547)"
       ```
     - *ICMPv6 (RS: 133, RA: 134, NS: 135, NA: 136) の監視*:
       ```bash
       tcpdump -i any -n -v "icmp6 and (ip6[40] == 133 or ip6[40] == 134 or ip6[40] == 135 or ip6[40] == 136)"
       ```
     - *PPPoE Discovery (PADI/PADO/PADR/PADS) および PPPセッション監視*:
       ```bash
       tcpdump -i any -e -n -vv "ether proto 0x8863 or ether proto 0x8864"
       ```
     - *MAP-E カプセル化パケット (IPv4-in-IPv6 / プロトコル番号 4) の監視*:
       ```bash
       tcpdump -i any -n -v "ip6 proto 4"
       ```

2. **`tshark`** (パッケージ: `tshark`)
   - **用途**: Wireshark の CUI 版。DHCPv6 Option 94 (MAP-E Container Option) のバイナリ構造や、PPP の LCP/IPCP ネゴシエーションの詳細をテキスト上で解析・抽出したい場合に強力。
   - **実行例**:
     ```bash
     # DHCPv6 パケット内の Option 94 フィールドを詳細表示
     tshark -i any -f "udp port 547" -V -Y "dhcpv6.option.type == 94"
     ```

3. **`conntrack-tools`** (パッケージ: `conntrack`)
   - **用途**: Linux カーネルの接続追跡テーブル（conntrack）のダンプ・監視。
   - **実行例**:
     ```bash
     conntrack -L -p tcp
     conntrack -E   # リアルタイムイベント監視
     ```

---

### D.2 プロトコル別検証・クライアントエミュレーションツール

1. **IPv6 / SLAAC / 近隣探索: `ndisc6` パッケージ (`rdisc6`, `ndisc6`)**
   - **用途**:
     - `rdisc6`: 指定した VLAN インターフェースに向けて Router Solicitation (RS) を送信し、RADVD から Router Advertisement (RA) が正しく返ってくるかテスト。
     - `ndisc6`: 指定した IPv6 アドレスに対して Neighbor Solicitation (NS) を送信し、Neighbor Advertisement (NA) 応答を確認。
   - **実行例**:
     ```bash
     # 指定 VLAN インターフェース宛てに RS を送り、RADVD の応答 RA を確認 (例: eth1.101)
     rdisc6 eth1.101

     # BRのアドレスに対する近隣探索応答を確認 (例: 2001:db8:1101::ff0a eth1.101)
     ndisc6 2001:db8:1101::ff0a eth1.101
     ```

2. **PPPoE 診断: `rp-pppoe` パッケージ (`pppoe` コマンド)**
   - **用途**: PPPoE Discovery (PADI) パケットを直接送出し、各 PPPoE サービスから PADO が返答されるか確認。
   - **実行例**:
     ```bash
     # 指定 VLAN 上で PADI をブロードキャストし、応答する PADO (AC名/サービス名) を一覧表示 (例: eth1.107)
     pppoe -A -I eth1.107

     # 特定サービス名（例: Service_A）を指定して応答確認
     pppoe -A -I eth1.107 -S Service_A
     ```

3. **DNS 解決・設定検査: `bind9-dnsutils` (`dig`, `named-checkconf`)**
   - **用途**: BIND9 のゾーン定義チェックや、プロビジョニング用ドメインの名前解決テスト。
   - **実行例**:
     ```bash
     # ローカル BIND コンテナに対して AAAA レコード解決確認
     dig @127.0.0.1 map.ocn.ad.jp AAAA +short

     # BIND 設定ファイルの構文検査
     named-checkconf bind/named.conf
     ```

4. **NTP 同期状態検査: `chrony` (`chronyc`)**
   - **用途**: NTP サーバーの同期状態、上流 NTP サーバーとのオフセット・遅延の確認。
   - **実行例**:
     ```bash
     chronyc tracking
     chronyc sources -v
     ```

5. **プロビジョニング & Web API 疎通: `curl`, `jq`, `openssl`**
   - **用途**: 付録Cの MAP-E プロビジョニングテスト（HTTPS 443）、Web 統合ダッシュボード（Port 1600）の REST API レスポンス検証。
   - **実行例**:
     ```bash
     # Webダッシュボード API テスト
     curl -s http://localhost:1600/api/clients | jq .
     curl -s http://localhost:1600/api/services | jq .

     # HTTPS 証明書の接続・内容検証
     openssl s_client -connect [2001:db8:9999::ff]:443 -servername map.ocn.ad.jp < /dev/null
     ```

---

### D.3 システム・カーネル・コンテナレベルの調査ツール

1. **ソケット・ポート競合調査: `iproute2` (`ss`), `lsof`**
   - **実行例**:
     ```bash
     ss -tulpn | grep -E ':(53|123|514|443|1600)'
     lsof -i :1600
     ```

2. **システムコール・権限エラー調査: `strace`**
   - **実行例**:
     ```bash
     strace -f -e trace=network,file,system -p <PID>
     ```

3. **ルーティング・ポリシー・トンネルインターフェース状態: `iproute2` 応用**
   - **実行例**:
     ```bash
     # MAP-E カプセル化トンネルルート確認
     ip route show dev mpe-common

     # 近隣探索 (NDP) キャッシュ確認
     ip -6 neigh show

     # VLAN インターフェース詳細情報 (パケット送受信数・エラー・ドロップ数、例: eth1.101)
     ip -d -s link show eth1.101
     ```

4. **VRF・PPP インターフェース状態確認**
   - **実行例**:
     ```bash
     # VRF デバイス一覧と所属インターフェースの確認
     ip -d link show type vrf
     ip link show master vrf-Service_A

     # PPP セッション一覧と接続中ユーザーの確認
     cat /var/log/pppoe-ip.log | tail -20
     ```

5. **Syslog-ng 集約疎通テスト: `logger`**
   - **実行例**:
     ```bash
     logger --server 127.0.0.1 --port 514 --tag test-debug "Debug syslog message test"
     ```

---

### D.4 Webダッシュボード UI・フロントエンドデバッグツール（Playwright等）

> [!NOTE]
> **通常運用時は不要**
> Playwright は Chromium 等のヘッドレスブラウザバイナリ（約300MB〜）や Node.js 実行環境を必要とするため、**サーバー本体の通常運用時には不要**です。
> ダッシュボードの UI 改修時やローカル開発端末での E2E 回帰テスト用途として一時的に利用することを推奨します。

1. **`Playwright` (Node.js)**
   - **用途**: CUI / SSH 環境において、ブラウザを開かずにダッシュボードの表示確認やスクリーンショット撮影を実施。
   - **事前準備**:
     ```bash
     cd ~/map-e_ocn/tools
     npm install -D playwright
     npx playwright install chromium --with-deps
     ```
   - **簡易スクリーンショット撮影ワンライナー**:
     ```bash
     npx playwright screenshot http://localhost:1600 test_dashboard.png
     ```
   - **エラー検出スクリプト実行**:
     ```bash
     node check_dashboard.mjs
     ```

2. **フロントエンド静的バリデータ (`tools/validate.mjs`, `tools/validate_css.mjs`)**
   - **実行例**:
     ```bash
     cd tools
     npm run validate
     ```

---

### D.5 デバッグ時の一時導入手順と作業後のアンインストール手順

#### 1. デバッグ時の一時インストール (Debian)
```bash
sudo apt-get update && sudo apt-get install -y \
  tcpdump \
  tshark \
  ndisc6 \
  bind9-dnsutils \
  conntrack \
  strace \
  lsof \
  rp-pppoe \
  jq \
  curl \
  python3-pip \
  python3-venv
```

#### 2. デバッグ完了後のアンインストール（通常運用環境への復元）
```bash
sudo apt-get purge -y \
  tcpdump \
  tshark \
  ndisc6 \
  bind9-dnsutils \
  conntrack \
  strace \
  lsof \
  rp-pppoe \
  jq \
  python3-pip \
  python3-venv
sudo apt-get autoremove -y
```

#### 3. Playwright / ブラウザバイナリキャッシュの削除
```bash
rm -rf ~/.cache/ms-playwright
cd ~/map-e_ocn/tools
npm uninstall playwright
```
