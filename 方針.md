# MAP-E（OCNバーチャルコネクト）仮想ISPエミュレーション環境 設計仕様書

## 0. 目的・前提

閉鎖環境（Debian Linux）上に、OCNバーチャルコネクト（MAP-E: RFC 7597 / RFC 7598）に準拠したISPプロビジョニングをエミュレートする試験環境を構築する。CE（CEルーター）に対しては、フレッツ回線のOCNが提供する以下4パターンのMAP-E接続を、それぞれ独立に試験できるようにする。

| # | 接続方式 | IPv4割当 | ひかり電話 |
|---|---|---|---|
| ① | SLAAC 動的IPv4 | 動的（共有プールから払い出し） | なし |
| ② | DHCPv6-PD 動的IPv4 | 動的（共有プールから払い出し） | あり |
| ③ | SLAAC 固定IPv4 | 固定（顧客指定IPv4、MAC→IPv4の対応表で管理） | なし |
| ④ | DHCPv6-PD 固定IPv4 | 固定（顧客指定IPv4、MAC→IPv4の対応表で管理） | あり |

4方式はそれぞれ挙動が異なるため混在させず、VLANで分離する（詳細は[2章](#2-wanインターフェースvlan設計)）。

**商用OCNとの相違点**：商用のMAP-E動的接続は複数CEで1つのIPv4アドレスを共有し、ポート数に制限がある。本環境では試験の簡便化のため、CE 1台につき専用の1 IPv4アドレス（`/32`）を1対1で割り当てる。CEに対してはOption 94経由で商用同様のポート制限があるように通知するが、BR（Debian）側では実際のポート制限は行わない（詳細は[4章](#4-option-94map-eルール生成ロジックflex-optionhost-reservation併用方式)）。

CEは動作確認用に YAMAHA RTX および NEC IX シリーズルーターを使用する想定。CE側の設定リファレンスは[付録A](#付録a-ceルーター設定リファレンス)を参照。

**Option 94計算方式は方式によって異なる**：動的接続方式（①②）は、Kea標準のHookライブラリ `libdhcp_flex_option.so` を用いて、**Keaが既に払い出し・重複回避を保証している値（DHCPv6-PDの`/56`払い出し）をそのままバイト単位で抜き出し、IPv4アドレスへ変換する**flex-option方式を採用する。Keaのアロケーションエンジンの重複回避保証にそのまま相乗りすることで、より確実にIPv4重複を防ぐ。一方、固定接続方式（③④）は「顧客が指定した特定のIPv4アドレスをそのままCEへ割り当てたい」という要件があり、flex-optionの式言語には算術演算がなく任意の値を注入できないため、Kea標準のHost Reservationごとの`option-data`個別指定機能でOption 94を直接指定する方式を採用する（詳細は[4章](#4-option-94map-eルール生成ロジックflex-optionhost-reservation併用方式)・[5章](#5-ipv4アドレス算出ロジックflex-optionhost-reservation併用方式)・[14章](#14-アドレスプール設計とflex-optionの実現性検証)を参照）。

---

## 1. 全体設計方針

* **同時接続想定数**：各方式ごとに、必要な台数分だけ任意のVLAN番号を追加できる設計とする。運用に応じてVLANを追加すれば台数を拡張できる（[2章](#2-wanインターフェースvlan設計)）。
* **ポート制限の通知と非強制**：CEに対してのみ、DHCPv6 Option 94経由でポート利用制限（ea-len / psid-len）を通知する。動的接続方式（①②）は商用OCNのポート制限を再現するため `ea-len = 8` / `psid-len = 8` / `offset-len = 4` を採用し、CEには実際にポート数制限がかかっているように通知する。固定接続方式（③④）は1台1IP占有型のため `ea-len = 0` とする。BR（Debian）側では実際のパケットフィルタリングやポート共有制御は行わず、1対1のカプセル化転送を行う（詳細は[4章](#4-option-94map-eルール生成ロジックflex-optionhost-reservation併用方式)）。
* **IPv6プレフィックス長の固定**：SLAAC系（①③）は `/64`、PD系（②④）は `/40`（CEへの委譲単位は `/56`）に固定する。
* **IA_NAの不使用**：WAN側IPv6グローバルアドレスのリース（`IA_NA`）は全方式で使用しない。CE-BR間の対向通信・DHCPv6（Information-Request）は、SLAAC系・PD系ともリンクローカルアドレス（`fe80::/10`）をベースに確立する。
* **VLANによるセグメント分離と専有**：4方式をVLANで分離する。特にSLAAC系（①③）はCE側の手動固定サフィックス衝突を避けるため、CE 1台につき 1 VLAN・1 プレフィックスを専有させる（詳細は[2章](#2-wanインターフェースvlan設計)）。

---

## 2. WANインターフェース・VLAN設計

WAN側は物理インターフェース `MAPE_IF`（`ens19`）1本をトランクとし、802.1Qタグ付きVLANで4方式のセグメントを分離する。VLAN IDは連番の範囲予約ではなく、`map-e.conf` 内の方式ごとの変数に**空白区切りで任意の番号を列挙**する方式とする（重複しなければ番号帯に制約はない）。

| 変数名 | 内容 | 現在の設定例 | 親インターフェース |
|---|---|---|---|
| `SLAAC_DYN_VLANS` | ①SLAAC動的用VLAN一覧 | `901 903 905` | `MAPE_IF` |
| `PD_DYN_VLANS` | ②DHCPv6-PD動的用VLAN一覧 | `911 913 915` | `MAPE_IF` |
| `SLAAC_FIX_VLANS` | ③SLAAC固定用VLAN一覧 | `921 923 925` | `MAPE_IF` |
| `PD_FIX_VLANS` | ④DHCPv6-PD固定用VLAN一覧 | `931 933 935` | `MAPE_IF` |

不要な方式は空欄にしてよい。

**VLANの追加手順**：
1. 対象方式の変数（例: `SLAAC_DYN_VLANS`）に、新しいVLAN番号をスペース区切りで追記する。
2. SLAAC系（①③）は、当該VLANのIPv6プレフィックス・BR側アドレスがビルド時に自動算出される（[3.2節](#32-ipv6プレフィックス採番規則)の採番規則により、VLAN番号ごとに動的に計算されるため、VLANごとの個別変数を追加する必要はない）。
3. PD系（②④）はVLANごとのプレフィックス追記は不要（PD系は方式単位で1つの委譲プールを共有するため）。

**なぜPD系はVLANを分けても単一プールでよいか**：DHCPv6-PDはCEごとに異なる `/56` が個別に委譲されるため、同一リンク上に複数CEが存在してもプレフィックス衝突が起きない。一方SLAACはCEが自分でIPv6アドレスの下位64ビット（サフィックス）を決めるため、同一リンクに複数CEを収容するとサフィックス固定運用時に衝突し得る。これがSLAAC系にVLAN分離が必要な理由である。

---

## 3. IPv6/IPv4 アドレス空間設計

### 3.1 方式別マトリクス（flex-option／Host Reservation併用方式）

| 方式 | IPv6ネットワーク空間 | CE側 配布・接続仕様 | IPv4アドレス空間（CE通知用） | アドレス決定ロジック |
|---|---|---|---|---|
| ① SLAAC動的 | VLANごとに個別の`/64`（採番規則は3.2） | SLAAC（RA）※対向通信はリンクローカル | `BR_IPV4_POOL`（全方式共有の動的プール） | Kea `flex-option` hookが、当該Kea subnet（＝VLAN）ごとに設定された「VLAN番号由来の固定バイト」＋クライアントDUIDの一部（`option[1].hex`）をそのまま`concat`して`/32`を合成 |
| ② DHCPv6-PD動的 | `PD_POOL`由来の`${BASE_SUBNET}:2000::/40`（全VLAN共有・`/56`ずつ委譲） | `/56`（PDプール）※`IA_NA`は不使用 | `BR_IPV4_POOL`（全方式共有の動的プール） | Kea `flex-option` hookが、クエリパケット中のIA_PD（`option[25].option[26].hex`）からKeaが払い出した`/56`の委譲ID部分（バイトオフセット固定・16bit＝2byte）をそのまま`substring`で抜き出し、固定バイトと`concat`して`/32`を合成。**Keaの`/40`プール内重複回避保証にそのまま相乗り（ハッシュ不使用）** |
| ③ SLAAC固定 | VLANごとに個別の`/64`（採番規則は3.2、①③で別プレフィックス帯） | SLAAC（RA）※対向通信はリンクローカル | `map-e-static-ip.conf`に登録されたMAC→固定IPv4（顧客指定値） | 登録済みMAC（`KNOWN`クラス）：Host ReservationごとのOption 94個別`option-data`として、事前計算した固定IPv4を含む完全なhex値を直接指定。未登録MAC（`UNKNOWN`クラス）：①と同一のflex-option式（VLAN番号＋DUIDの一部）へ自動フォールバック |
| ④ DHCPv6-PD固定 | `/56`はHost Reservationによる固定払い出しを行わず、②と同一の`PD_POOL`から都度動的に採番（Kea重複回避保証をそのまま利用） | `/56`（②と同一プールから動的払い出し）※`IA_NA`は不使用 | `map-e-static-ip.conf`に登録されたMAC→固定IPv4（顧客指定値） | 登録済みMAC（`KNOWN`クラス）：Host ReservationごとのOption 94個別`option-data`として、固定IPv4を含む完全なhex値を直接指定。Rule IPv6 Prefix部分は`${BASE_SUBNET}:${PD_FIX_POOL}::/56`（委譲ID`0000`固定）の**ダミー値**とし、実際に委譲される`/56`とは一致させない（CEがRule IPv6 Prefixと実アドレスの整合性を厳密チェックしない前提。詳細は[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）。未登録MAC（`UNKNOWN`クラス）：②と同一のflex-option式へ自動フォールバック |

各変数の現在の値は[12章](#12-map-econf-パラメータ一覧)を参照。

**③④固定IPv4の管理とフォールバック**：
* **固定IPv4管理**：顧客指定の固定IPv4を割り当てるため、`map-e-static-ip.conf`（MAC→IPv4対応表）を参照し、Kea Host Reservationの `option-data` でOption 94を個別に指定する。
* **フォールバック**：`map-e-static-ip.conf` 未登録のMACが固定セグメント（③④）に接続した場合は、Keaの `UNKNOWN` ビルトインクラスにより、①②と同じflex-option式および動的プールへ自動フォールバックする。

※固定指定の詳細構造や `PD_POOL` 共有ロジックは [4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定) を参照。

### 3.2 IPv6プレフィックス採番規則

以下のベース値（`map-e.conf` で定義）とVLAN IDから**都度計算**する。

* SLAAC動的：4番目のhextetを `SLAAC_BR_BASE`（`1000`）＋ VLAN ID とする（`${BASE_SUBNET}:1<VLAN ID>::/64`）
* SLAAC固定：4番目のhextetを `SLAAC_FIX_BR_PREFIX`（`3000`）＋ VLAN ID とする（`${BASE_SUBNET}:3<VLAN ID>::/64`）
* PD動的：`${BASE_SUBNET}:${PD_POOL}::/40`（`PD_POOL=2000` 固定、VLANによらず共通）
* PD固定：Host Reservationによる固定`/56`払い出しは行わないため、実際の委譲プレフィックスは`PD_POOL`（②と同一）から都度動的に採番される。`PD_FIX_POOL`（`4000`）は実在のIPv6委譲には使用せず、Option 94のRule IPv6 Prefix欄に埋め込む**ダミー値のネットワーク部**としてのみ使用する（`${BASE_SUBNET}:${PD_FIX_POOL}::/56`、委譲ID部分は`0000`固定。詳細は[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）

例：VLAN 901（SLAAC動的）→ `5f00:3aa:1901::/64`、VLAN 941（SLAAC固定として使う場合）→ `5f00:3aa:3941::/64`。

### 3.3 BR側アドレスの命名規則（SLAAC系）

各VLANのBR側アドレス（RA送信元・対向リンクローカルの代替として使うGUA）は、プレフィックスの末尾に `map-e.conf` で定義するサフィックスを付与する。

* SLAAC動的：`<このVLANのプレフィックス>${SLAAC_BR_SUFFIX}` → `::ff0a`
* SLAAC固定：`<このVLANのプレフィックス>${SLAAC_FIX_BR_SUFFIX}` → `::ff0a`

`map-e.conf` では動的・固定とも同一のサフィックス値（`::ff0a`）が設定されているが、プレフィックス自体（`1<VLAN ID>` 系／`3<VLAN ID>` 系）が異なるため、アドレスが衝突することはない。

PD系（②④）はリンクローカルのみで対向するため、この命名規則は適用されない。

---

## 4. Option 94（MAP-Eルール）生成ロジック（flex-option／Host Reservation併用方式）

### 4.0 方式概要

動的接続方式（①②）では、Kea標準Hookライブラリ `libdhcp_flex_option.so` を `kea-dhcp6` にロードし、Option 94（`OPTION_S46_CONT_MAPE`, code 94）の値そのものを、**Kea設定ファイル内の式（expression）評価結果**として`supersede`する（`kea-map-e-hook` ルーティング注入用途で使用、[9章](#9-プロジェクト構成)参照）。

固定接続方式（③④）では、顧客が指定した特定のIPv4アドレスをそのままCEへ割り当てる必要があるため、flex-optionの式（バイト列の切り出しのみで算術演算不可）ではなく、**Kea標準のHost Reservationごとの`option-data`個別指定機能**を使ってOption 94の値を丸ごと固定hexとして設定する（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）。登録されていないMACはKeaのビルトインクラス`UNKNOWN`に自動分類され、①②と同一のflex-option式にフォールバックする。

flex-optionの式は **クエリパケット（CEからの要求パケット）に対して評価される**（レスポンスパケット自体は式から参照できない）。DHCPv6-PDでは、Request/Renew/RebindなどのクエリにクライアントがコピーしてくるIA_PD（`option[25]`）のIAPREFIXサブオプション（`option[26]`）に、直前のAdvertise等でKeaが払い出した`/56`がそのまま含まれるため、これを抜き出せば「Keaが払い出し済みの重複しない値」を流用できる。ただし**最初のSolicit→Advertiseの一往復目だけは、クエリ側にまだ確定値が入っていない**（Solicitのヒントのみ）ため、Advertise中のOption 94は不正確になりうる。実運用上問題になるのはRequest→Reply以降（実際のリース確定後）の値であり、CEも通常はReplyの内容を採用するため実害は小さいが、既知の制約として明記する。

またflex-optionの式言語は `substring` / `concat` / `ifelse` / `hexstring` / `intNtotext` などの**バイト単位の文字列操作のみ**で、加算・剰余（mod）・ビットシフトといった**算術演算は提供されない**（[14章](#14-アドレスプール設計とflex-optionの実現性検証)で検証）。そのため本方式で組み立てられるのは「クエリパケット中の既存バイト列を、そのまま切り出して並べ替える」ことに限られる。

### 4.1 EAビット長の採用値

商用OCNバーチャルコネクト（動的）の仕様を再現するため、動的接続方式（①②）は **ea-len = 8** を採用する。固定接続方式（③④）は1台1IP占有型のため **ea-len = 0** とする。flex-option化により、`ea-len`はKea設定（`kea-dhcp6.conf.tmpl`内の`options`パラメータ）に接続方式ごとの固定値としてそのまま記述するため、従来の単独生成実装にあった「`ea-len`が常に`0x00`になる」バグは構造的に発生しなくなる（スクリプトの条件分岐ではなく、Subnet／Client Classごとに異なる式を割り当てるKea標準の設定機構で表現するため）。

参考: ea-len を変えた場合の商用仕様上の違い

| 項目 | ea-len = 8（商用・本環境採用） | ea-len = 4（参考） |
|---|---|---|
| 1つのIPを共有する人数 | 約240台 | 15台 |
| 1台が使えるポート数 | 240個 | 3,840個 |

### 4.2 動的接続方式（①②）のパラメーター

| 項目 | 値 |
|---|---|
| Rule IPv4 Prefix | flex-optionで合成した個別の `IPv4アドレス/32` |
| EAビット長（ea-len） | `8` |
| PSID長（psid-len） | `8` |
| オフセット長（offset-len） | `4` |

Rule IPv4 Prefixが `/32` 固定であるため、CE側でEAビットによるIPv4アドレスの合成変形は発生せず、flex-optionで合成されたIPv4アドレスがそのままWAN側に適用される。一方でPSID（ポートセット）はCE側で商用同様に計算され、CE内部では1アドレスあたり240ポートに制限された状態でMAP-Eトンネルが確立される。

**BR側はこの制限を実施しない**：BR（Debian）はポート番号を不問として、CEから届いたカプセル化パケットを全て1対1でルーティング・転送する（行き・戻り方向とも）。CEへの割り当て確定時（HTTPS要求時／NDP監視による検出時）、割り当てた `IPv4/32` 宛のトラフィックが共通のトンネルインターフェース `mpe-common` へ向き、かつカプセル化されるよう、`encap ip6` パラメータ（送信元 `BR_IPV6`、宛先 `ce_ipv6`）を指定したルートをBR側ルーティングテーブルへ動的に注入する。なお、`ce_ipv6` の Interface ID（後半64ビット）生成において、動的接続方式（①②）では CE 側の計算結果と対向アドレスを一致させるため、VLAN番号やプレフィックスヘクステットから `target_psid`（8ビット）を動的に算出し、Interface ID の PSID フィールド（block7: `target_psid` + `00`）へ埋め込む（固定接続方式③④はポート制限なしのため `target_psid = 0` となる。[5章](#5-ipv4アドレス算出ロジックflex-optionhost-reservation併用方式)参照）。

### 4.3 固定接続方式（③④）のパラメーター

| 項目 | 値 |
|---|---|
| Rule IPv4 Prefix | `map-e-static-ip.conf`で登録された顧客指定IPv4の`/32`（MACごとにHost Reservationの`option-data`として個別指定） |
| EAビット長（ea-len） | `0` |

固定IP用Option 94（Type 89 Rule Option）は、動的方式のようなKea式評価ではなく、**MACごとに事前計算した完全なhex値**をHost Reservationの`option-data`として直接設定する（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）。固定値は`EA-len`（`0x00`）と`IPv4 Prefix Length`（`0x20`＝32固定）を含め、Rule Option全体を1つのhex文字列として組み立てる。

### 4.4 セグメント別の生成ロジック

| 方式 | トリガー | Rule IPv6 Prefix | Rule IPv4 Prefix | 未登録時の挙動 |
|---|---|---|---|---|
| ① SLAAC動的 | CEからのInformation-Request受信 | 該当VLANのIPv6プレフィックス | 固定値（VLAN由来、Kea subnet単位でハードコード）＋`substring(option[1].hex, ...)`（DUIDの一部）をflex-option式で合成 | ―（全CE対象） |
| ② PD動的 | CEへの`/56`払い出し | 払い出された`/56`（`PD_POOL`由来） | 固定値＋`substring(option[25].option[26].hex, 14, 2)`（Keaが払い出した`/56`の委譲ID部分）をflex-option式で合成 | ―（全CE対象） |
| ③ SLAAC固定 | CEからのInformation-Request受信 | 該当VLANのIPv6プレフィックス（実IPv6、ダミーではない） | `map-e-static-ip.conf`登録済みMAC：Host Reservationの`option-data`でOption 94を丸ごと固定指定（顧客指定IPv4を含む） | 未登録の場合は`UNKNOWN`クラスにより①と同一のflex-option式・同一プールへ自動フォールバック |
| ④ PD固定 | CEへの`/56`払い出し（②と同一プールから動的採番） | `${BASE_SUBNET}:${PD_FIX_POOL}::/56`（委譲ID`0000`固定の**ダミー値**。実際に委譲される`/56`とは一致しない） | `map-e-static-ip.conf`登録済みMAC：Host Reservationの`option-data`でOption 94を丸ごと固定指定（顧客指定IPv4を含む） | 未登録の場合は`UNKNOWN`クラスにより②と同一のflex-option式・同一プールへ自動フォールバック |

動的方式（①②）のKea設定上のイメージ（`kea-dhcp6.conf.tmpl`内、`hooks-libraries`の`libdhcp_flex_option.so`パラメータ、`UNKNOWN`クラスにも同じ式を適用）：

```jsonc
{
  "code": 94,
  "supersede": "concat(<BR用16byte固定hex>, concat(<Rule flags+ea-len+prefix4-len 固定hex>, concat(<IPv4固定上位2byte>, concat(substring(option[25].option[26].hex, 14, 2), <prefix6-len+IPv6prefix 固定/可変hex>))))",
  "csv-format": false
}
```

固定方式（③④）のKea設定上のイメージ（`reservations`内、MACごとの`option-data`）：

```jsonc
{
  "hw-address": "<登録MAC>",
  "option-data": [
    {
      "code": 94,
      "csv-format": false,
      "data": "<事前計算済み完全固定hex文字列（顧客指定IPv4を含む）>"
    }
  ]
}
```

### 4.5 固定接続方式（③④）のOption 94／Host Reservation個別指定

**方式選定の背景**：固定接続方式（③④）は「顧客が指定した特定のIPv4アドレスをそのままCEへ割り当てたい」という要件がある。[4.0節](#40-方式概要)および[14.2節](#142-flex-option式言語の制約確認結果)で述べたとおり、flex-optionの式言語には算術演算がなくクエリパケット内の既存バイト列の切り出ししか行えないため、運用者が任意に指定した固定IPv4を動的に組み込むことができない。そのため③④ではflex-optionを使わず、Kea標準のHost Reservation個別 `option-data` 機能でOption 94の値そのものを直接指定する。

**優先順位の根拠**：Kea公式ドキュメントにおいて、option-dataの優先順位は次の通り明記されている（DHCPv4/DHCPv6共通）。

> Kea options precedence order is (from most important to least): host reservation, pool, subnet, shared network, class, global.

またKeaは、Host Reservationに一致したクライアントを自動的にビルトインクラス`KNOWN`へ、一致しないクライアントを`UNKNOWN`へ分類する。この2つの性質を組み合わせることで、次の構成が実現できる。

* **登録済みMAC（`KNOWN`）**：host reservationのoption-dataが最優先で適用されるため、`map-e-static-ip.conf`から生成したreservation個別のOption 94固定hexがそのままCEへ通知される。
* **未登録MAC（`UNKNOWN`）**：該当するreservationが存在しないため、Subnet（またはshared-network）に明示的に紐付けた`UNKNOWN`クラスのoption-data（＝①②と同一のflex-option式）が適用される。

なお`UNKNOWN`クラスは自動生成されるがoption-dataは自動では紐付かないため、`kea-dhcp6.conf.tmpl`側で③④のSubnet／shared-networkに`client-classes`として`UNKNOWN`向けのoption-data定義を明示的に持たせる必要がある。

**④のRule IPv6 Prefixをダミー値にする理由**：④はIPv6 `/56`をHost Reservationで固定払い出しせず、②と同一のKea PDプールから都度動的に採番する（OCN商用でもIPv6アドレス動的）。一方でOption 94のRule IPv6 Prefixフィールドは、reservationのoption-dataとして静的にしか書けないため、都度変わる実際の委譲`/56`を反映できない。この矛盾を解消するため、Rule IPv6 Prefixには実際の委譲`/56`とは異なる**ダミー値**（`${BASE_SUBNET}:${PD_FIX_POOL}::/56`、委譲ID部分`0000`固定）を使用する。CEがOption 94のRule IPv6 PrefixとMAP-Eトンネルの実IPv6アドレスとの整合性を厳密にチェックしない前提の割り切りであり、要検証事項として付記する。

**Kea Host Reservation生成時のOption 94 hex事前計算内訳（③④共通の骨格）**：

| フィールド | 値 | 備考 |
|---|---|---|
| OPTION_S46_BR (type90, len16) | `BR_IPV6`固定値 | [6章](#6-br内部インターフェース)の生成式で計算済みの固定値 |
| OPTION_S46_RULE ヘッダ flags | `0x00` | |
| ea-len | `0x00` | ③④は1台1IP専有 |
| prefix4-len | `0x20`(32) | |
| Rule IPv4 Prefix | **MAC個別の固定IPv4**（`map-e-static-ip.conf`の値をhex変換） | 顧客指定値 |
| prefix6-len | ③:`0x40`(64) ／ ④:`0x38`(56) | |
| Rule IPv6 Prefix | ③:該当VLANのプレフィックス（VLAN由来の実在の値） ／ ④:`${BASE_SUBNET}:${PD_FIX_POOL}::`（委譲ID`0000`固定のダミー値） | ③は実値、④はダミー |

**③のVLAN依存性について**：③(SLAAC固定)はVLANごとにIPv6プレフィックスが異なるため、同一MACのCEでも接続先VLANによってOption 94のIPv6部分（Rule IPv6 Prefix）が変わる。したがって③のreservationはSubnet（VLAN）をまたいで使い回せず、Subnetごとに個別のreservationとして生成する（設計上1VLAN=1CEのため、そのVLANで使用する予定のMAC1件を紐付ける運用となる）。

**④のVLAN非依存性について**：④(PD固定)はRule IPv6 Prefixがダミー固定値であるため、VLAN（Subnet）に依存しない。reservationはshared-network直下に1回だけ登録すれば、どのPD固定VLANに接続しても同一のOption 94が適用される。

---

## 5. IPv4アドレス算出ロジック（flex-option／Host Reservation併用方式）

Kea（動的方式：flex-option／固定方式：Host Reservation個別指定）と `mape-provisioning-server`／`mape-route-monitor`（BR側ルーティング注入）の間で一貫した割り当てを行うため、**両者が同一のロジックを、それぞれの言語（Kea式言語・Kea設定 / Perl・Python）で独立に実装**する。共通ライブラリとして計算式そのものを共有することはできない（Kea側は式言語または静的設定、BR側の`mape_calc`はスクリプト言語のため）ので、本章の定義を「唯一の正」とし、両実装がこれに一致することをテストで担保する。

* **PD動的（②）**：クエリパケットのIA_PD（`option[25].option[26].hex`、IAPREFIXサブオプション全25byte：preferred-lifetime 4byte＋valid-lifetime 4byte＋prefix-length 1byte＋prefix address 16byte）から、Keaが払い出した`/56`のうち「`/40`プールに対して可変となる16bit（＝2byte、`/40`と`/56`はいずれも8の倍数ビットのためバイト境界に一致する）」を`substring(option[25].option[26].hex, 14, 2)`で抜き出す。この2byteをそのままIPv4アドレスの下位16bitとして採用し、上位16bit（`BR_IPV4_POOL`のネットワーク部）と`concat`して`/32`を合成する。**Keaの`/40`プール内での重複回避保証にそのまま相乗りする。**
* **SLAAC動的（①）**：SLAACではKeaによるアドレス払い出し（IA_NA）が発生しないため、②のような「Kea自身の重複回避保証」に相乗りできる元データが存在しない。そこで、設計上すでにVLAN 1つにつきCE 1台を専有させている（[2章](#2-wanインターフェースvlan設計)）ことを利用し、**VLAN番号（Kea subnet単位で固定値としてハードコード）を上位バイト、クライアントDUIDの一部（`substring(option[1].hex, ...)`）を下位バイトとして`concat`**し、`BR_IPV4_POOL`の上位16bitと組み合わせて`/32`を合成する。VLAN番号の部分は原理的にVLANごとに一意（設計上1VLAN=1CE）だが、DUID部分の追加はハッシュではなく単なる切り出しのため、Kea側で重複を保証する仕組みではない点に注意（運用上は「テスト環境でDUIDが重複しない」という前提に依存する）。
* **固定セグメント（③④共通）**：`map-e-static-ip.conf`は「MACアドレス→顧客指定の固定IPv4」の対応表であり（フォーマットは `MACアドレス,固定IPv4/プレフィックス長` のカンマ区切り）、IPv6（`/56`や`/64`）は記載しない（OCN商用でもIPv6アドレスは変動しうるため、本環境の固定運用対象はIPv4のみとする）。登録されたMACに対しては、Kea Host Reservationの`option-data`でOption 94を丸ごと固定hexとして直接指定する（式評価ではなく静的な値の埋め込み。[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）。未登録の場合は接続を拒否せず、Keaのビルトインクラス`UNKNOWN`により②・①と同じ式・同じプールへフォールバックする。④のIPv6 `/56`自体は②と同一のKeaプールから都度動的に採番されるため、**Keaのプール内重複回避保証がそのまま働き、IPv4重複のリスク管理を別途行う必要はない**（[14章](#14-アドレスプール設計とflex-optionの実現性検証)参照）。一方、固定IPv4自体の重複回避はKeaの機構に相乗りできないため、`map-e-static-ip.conf`の登録内容を運用者が自前で管理する。

**CE IPv6アドレス（データ通信用）の合成**：CEがデータ通信のカプセル化送信元として使うIPv6アドレスをBR側（`mape_calc`）でも同一ロジックで算出し、トンネル対向アドレスとして設定する。割り当てた `IPv4/32` を16進数に変換し、RFC 7597 / OCN仕様に準拠した形式でインターフェースID（後半64ビット）へ埋め込む。動的接続方式（①②）では、VLAN番号やプレフィックスヘクステットから `target_psid`（8ビット）を動的に算出し、Interface ID の PSID フィールド（block7: `target_psid` + `00`）へ組み込んで対向 `ce_ipv6` を生成する（固定接続方式③④は `target_psid = 0`）。

**トンネルデバイス構成**：対向ごとに個別にトンネルを作るのではなく、システム全体で1つの共通デバイス `mpe-common` を共有して動作する。`ip6tnl` の `external` モードを利用し、対向ごとのカプセル化パラメータ（src/dst）はルート単位で `encap ip6` として設定する。

---

## 6. BR内部インターフェース

CE収容用のVLAN群（[2章](#2-wanインターフェースvlan設計)）とは別に、BR（Debian）内部で完結する2つの仮想インターフェースを設ける。物理ポートには接続しない。

| インターフェース | 変数名 | プレフィックス | 用途 |
|---|---|---|---|
| `dummy0` | `BR_IF` | `BR_PREFIX`（`/96`） | MAP-Eトンネル終端（BRのIPv6/IPv4エンドポイント） |
| `dummy1` | `PROV_IF` | `MAPE_PROV_PREFIX`（`/64`） | DNS(`::53`)・NTP(`::123`)・プロビジョニングサーバー(`::ff`)の共通サービス基盤 |

`dummy1` はWAN側の全VLANからルーティングで到達可能とし、DNS/NTP/provisioning-serverはVLANごとに個別のアドレスを持たない（全方式・全VLAN共通の単一アドレスを使う）。各変数の現在の値は[12.2](#122-br内部インターフェースmap-e共通)を参照。

**dummy0のIPv6アドレス合成（Option 94 Type 90 / BR IPv6）**：OCN商用仕様に準拠し、RFC 6052形式でIPv4を埋め込んで自動生成する。この計算だけは値を追った方が理解しやすいため、現在の値での計算例を示す。

* 生成式：`BR_PREFIX（96bit）` + `BR_IPV4_ADDR（32bit、16進変換）`
* 計算例（現在の値）：`BR_PREFIX = 5f00:3aa:a001::/96`、`BR_IPV4_ADDR = 100.127.255.255`（16進: `647f:ffff`）→ `BR_IPV6 = 5f00:3aa:a001::647f:ffff`

CEはこのアドレスをトンネル対向（宛先）として認識し、カプセル化パケットを送信する。Debian側も同アドレスを `dummy0` のトンネル`local`アドレスとしてバインドし、カプセル化を解除する。

---

## 7. セグメント・インターフェースの3層分類

| 層 | 内容 |
|---|---|
| **① CE収容インターフェース（アクセスVLAN層）** | BRが各方式のCE群を直接収容し対向通信を行うVLAN群（[2章](#2-wanインターフェースvlan設計)）。SLAAC系はRAでプレフィックス通知しCEが自動アドレス取得、PD系はリンクローカルのみで対向しCEへプレフィックスを委譲。 |
| **② BR内部仮想インターフェース（コア・サービス層）** | `dummy0`（トンネル終端）・`dummy1`（DNS/NTP/provisioning共通サービス）（[6章](#6-br内部インターフェース)）。 |
| **③ CE配下ネットワーク（宅内LAN層）** | CEがIA_PDで取得したプレフィックス（動的または固定の`/56`）をLAN側にさらに分配（通常`/64`）し、宅内端末の通信を模擬する試験対象領域。 |

---

## 8. 動作シーケンス

### ひかり電話なし・動的IPv4（SLAAC、①）
1. CEはSLAACでWAN側IPv6アドレスを取得する（provisioning-serverへの接続用）。
2. DHCPv6のInformation-RequestでDNS・NTP・domain search listを取得する。
3. DNSを引いてprovisioning-serverへアクセスし、MAP-Eトンネル用のIPv6アドレスおよびIPv4アドレスを取得する。
4. provisioning-serverは、払い出した情報に従って対向側のルーティングを自動設定する。
5. CEは受け取ったMAP-E接続用IPv6アドレスでMAP-Eトンネル（ip6tnl）を作成する。
6. CEのトンネルインターフェースに、取得したIPv4アドレスを付与する。

### ひかり電話あり・動的IPv4（DHCPv6-PD、②）
1. RAでリンクローカルのデフォルトルートを流す。
2. DHCPv6（IA_PD）でLAN用プレフィックス、およびDNS・NTP・domain search listを取得する。
3. 以降は①と同様の手順でMAP-Eトンネルを確立する（BR-CE間はリンクローカルアドレスで通信、デフォルトルートはRAを受信したリンクローカルアドレス経由で設定される）。

※RAの動作が①③と②④で異なるため、SLAAC系とDHCPv6-PD系は必ず別VLANにする。

---

## 9. プロジェクト構成

```
map-e_ocn/
├── map-e.conf                  # 中央設定ファイル（全パラメータ定義）
├── map-e-static-ip.conf        # 固定IPv4マッピング (MAC→顧客指定IPv4。IPv6は記載しない)
├── Makefile                    # テンプレート展開・インストール・パッケージ管理
├── 方針.md                     # 本ドキュメント（仕様書）
├── OCN_設定簡単まとめ.md       # リファレンス・設定要約
├── verify_calc.py              # 計算ロジック検証用テストスクリプト
│
├── generator/                  # ビルド・設定生成用Perlスクリプト群
│   ├── MapeCommon.pm           # 共通Perlモジュール (環境変数処理・自動計算)
│   ├── gen_kea_configs.pl      # Kea DHCPv6 設定生成スクリプト
│   ├── gen_bind_configs.pl     # BIND9 設定生成スクリプト
│   ├── gen_system_mod.pl       # システム/ネットワーク設定生成スクリプト
│   ├── gen_radvd_mod.pl        # radvd 設定生成スクリプト
│   ├── gen_chrony_config.pl    # chrony NTP 設定生成スクリプト
│   └── gen_mape_mod.pl         # mape-provisioning 関連生成スクリプト
│
├── CE-setting/                 # CEルーター(NEC IX2215)の設定例・ログ
│   ├── CPE.txt                 # 基本設定例
│   ├── CPE-PD-dynamic.txt      # DHCPv6-PD動的設定例
│   ├── CPE-RA-dynamic.txt      # SLAAC動的設定例
│   └── CPE-command-list.txt    # 確認・デバッグコマンド集
│
├── system/                     # ネットワーク設定
│   ├── interfaces              # 生成済み設定 → /etc/network/interfaces
│   └── 99-network-routing.conf # 生成済み設定 → /etc/sysctl.d/
│
├── radvd/                      # Router Advertisement
│   └── radvd.conf              # 生成済み設定 → /etc/radvd.conf
│
├── kea-dhcp6/                  # DHCPv6 サーバー
│   ├── kea-dhcp6.conf.tmpl     # テンプレート（①②はlibdhcp_flex_option.soのoptions式、③④はreservations配下のoption-data。[13章](#13-flex-option-詳細仕様)参照）
│   ├── kea-dhcp6.conf          # 生成済み設定 → /etc/kea/kea-dhcp6.conf
│   ├── kea-map-e-hook.tmpl     # テンプレート (Kea hookスクリプト。ルーティング注入・削除用)
│   ├── kea-map-e-hook          # 生成済み → /usr/local/bin/kea-map-e-hook
│   └── kea.sudoers             # Sudoers設定 → /etc/sudoers.d/kea
│
├── apparmor/                   # AppArmor プロファイル（テンプレートなし、そのままコピー）
│   ├── usr.sbin.kea-dhcp6      # → /etc/apparmor.d/usr.sbin.kea-dhcp6
│   └── usr.sbin.kea-lfc        # → /etc/apparmor.d/usr.sbin.kea-lfc
│
├── bind/                       # DNS (BIND9)
│   ├── named.conf.local.tmpl   # テンプレート
│   ├── named.conf.local        # 生成済み → /etc/bind/named.conf.local
│   ├── db.map.ocn.ad.jp.tmpl   # テンプレート (OCNゾーン)
│   ├── db.map.ocn.ad.jp        # 生成済み → /etc/bind/db.map.ocn.ad.jp
│   ├── db.v6connect.net.tmpl   # テンプレート (v6connectゾーン)
│   └── db.v6connect.net        # 生成済み → /etc/bind/db.v6connect.net
│
├── chrony/                     # NTP (chrony)
│   ├── chrony.conf.tmpl        # テンプレート
│   └── chrony.conf             # 生成済み → /etc/chrony/chrony.conf
│
├── syslog-ng/                  # ログ出力・ローテーション設定
│   ├── mape.conf               # 生成済み/配置対象 → /etc/syslog-ng/conf.d/mape.conf
│   └── mape.logrotate          # ログローテーション設定 → /etc/logrotate.d/mape
│
└── mape-provisioning-server/   # MAP-E 設定配布・ルーティング
    ├── mape-provisioning-server.tmpl         # テンプレート
    ├── mape-provisioning-server              # 生成済み → /usr/local/bin/
    ├── mape-provisioning-server.service.tmpl # テンプレート
    ├── mape-provisioning-server.service      # 生成済み → /etc/systemd/system/
    ├── mape-route-monitor.tmpl               # テンプレート
    ├── mape-route-monitor                    # 生成済み → /usr/local/bin/
    ├── mape-route-monitor.service.tmpl       # テンプレート
    ├── mape-route-monitor.service            # 生成済み → /etc/systemd/system/
    ├── mape_calc.tmpl                        # 共通ライブラリテンプレート
    ├── mape_calc.py                          # 生成済み → /usr/local/bin/mape_calc.py
    ├── success_slaac.json                    # レスポンスサンプル (SLAAC)
    ├── success_pd.json                       # レスポンスサンプル (PD)
    └── success_examples.md                   # 配布レスポンス仕様・解説
```

インターフェースや各種アドレスは `./map-e.conf` に一元定義し、すべての設定ファイル・スクリプトはこれに従う。

---

## 10. ビルドシステム（Makefile）

**設定生成・テンプレート展開の仕組み**：
* [map-e.conf](file:///home/abc123/map-e_ocn/map-e.conf) は [Makefile](file:///home/abc123/map-e_ocn/Makefile) 内で環境変数へエクスポートされ、各生成処理で読み込まれる（`generator/` 配下のスクリプトは [MapeCommon.pm](file:///home/abc123/map-e_ocn/generator/MapeCommon.pm) の共通処理によりシェル変数展開の解決や派生変数の自動計算を行う）。
* 各設定ファイル・スクリプトの生成は、`generator/` 配下の Perl スクリプト群を呼び出すことで実行される：
  * **プログラムによる直接出力**（テンプレート不使用）：
    * `system/interfaces` および `system/99-network-routing.conf` — [gen_system_mod.pl](file:///home/abc123/map-e_ocn/generator/gen_system_mod.pl) が `SLAAC_DYN_VLANS` などの変数から VLAN ごとの設定ブロックを動的に組み立てて出力する。
    * `radvd/radvd.conf` — [gen_radvd_mod.pl](file:///home/abc123/map-e_ocn/generator/gen_radvd_mod.pl) が VLAN 設定やプレフィックス情報を基に直接構成を出力する。
  * **テンプレートファイル（`.tmpl`）の置換展開**：
    * `chrony/chrony.conf` — [gen_chrony_config.pl](file:///home/abc123/map-e_ocn/generator/gen_chrony_config.pl) が `chrony.conf.tmpl` を読み込み、`SYSTEM_NTP` を複数行の server 記述に変換して埋め込む。
    * `bind/named.conf.local`, `bind/db.map.ocn.ad.jp`, `bind/db.v6connect.net` — [gen_bind_configs.pl](file:///home/abc123/map-e_ocn/generator/gen_bind_configs.pl) が対応するゾーンテンプレートを置換展開する。
    * `kea-dhcp6/kea-dhcp6.conf` — [gen_kea_configs.pl](file:///home/abc123/map-e_ocn/generator/gen_kea_configs.pl) が `kea-dhcp6.conf.tmpl` をベースに、`KEA_INTERFACES` や `KEA_SUBNET6_BLOCKS` 等を差し替えて生成する。
    * `kea-dhcp6/kea-map-e-hook` — [gen_kea_configs.pl](file:///home/abc123/map-e_ocn/generator/gen_kea_configs.pl) が `kea-map-e-hook.tmpl` の中身をそのまま出力する（置換処理なし）。
    * `mape-provisioning-server/` 配下の `mape_calc`, `mape-route-monitor`, `mape-provisioning-server` および systemd サービス定義（`*.service`） — [gen_mape_mod.pl](file:///home/abc123/map-e_ocn/generator/gen_mape_mod.pl) が各テンプレートファイルの `${変数名}` 部分を環境変数で置換して出力する。
  * **openssl による直接生成**：
    * `mape-provisioning-server/server.crt` および `server.key` は [Makefile](file:///home/abc123/map-e_ocn/Makefile) 内で `openssl req` コマンドにより直接生成される。

**Makeターゲット**：

| ターゲット | 実行可能ユーザー | 説明 |
|---|---|---|
| `make` / `make generate` / `make all` | 一般ユーザー (`check-user` が強制) | `map-e.conf` の定義に基づき、すべての設定ファイル・スクリプトを生成 |
| `make check` | root 権限 (`check-root` が強制) | chrony、syslog-ng、BIND（ゾーン定義含む）、radvd、Kea DHCPv6 の構文チェックを実行 |
| `make install` | root 権限 (`check-root` が強制) | 生成ファイルをシステムディレクトリへインストールし、必要なサービスを再起動または有効化・ロード |
| `make packages` | root 権限 (`check-root` が強制) | radvd、kea-dhcp6-server、bind9、chrony 等の必要パッケージが導入されているか確認し、未導入なら apt-get でインストール |
| `make archive` | 一般ユーザー (`check-user` が強制) | ネットワーク設定状態を `STATUS.TXT` に出力した上で、ソースコード一式を `MAP-E.shar` (shar) および `MAP-E.tar.xz` に圧縮アーカイブ化 |
| `make clean` | 制限なし | 生成された設定ファイル群およびアーカイブファイル、状態出力ファイルを削除 |

**実行ユーザー制限（check-user / check-root）**：
* 一般ユーザーでのみ実行できるターゲット (`generate`, `archive`) と、root 権限が必須なターゲット (`check`, `install`, `packages`) を Makefile 内の `check-user` / `check-root` タスクで切り分け、誤操作を防ぐ。

**install時の差分検知とサービス反映**：
* `/etc/network/interfaces` は `cmp -s` により生成ファイルと現設定に差分がある場合のみ上書きされ、ifdown/ifup または networking サービス再起動で再ロードされる。
* 各種 systemd サービス（`mape-provisioning-server.service`、`mape-route-monitor.service` 等）の配置後には `systemctl daemon-reload` を実行し、サービスを有効化・再起動する。
* BIND (rndc reload) や radvd (systemctl reload) などのサービスは、システム構成に応じてリロードまたは再起動が行われる。

---

## 11. 設定対象ファイル一覧

1. **ネットワーク設定**（`./system/`）
   * `interfaces` — インターフェース設定（管理用、SLAAC系VLAN群、PD系VLAN群、`dummy0`/`dummy1`）
   * `sysctl.conf` — カーネルパラメータ（IPv4/IPv6転送、rp_filter無効化、broadcast ping応答）

2. **Router Advertisement**（`./radvd/`）
   * `radvd.conf` — 方式ごとに異なるRA動作
     * SLAAC系VLAN群（①③）：プレフィックス通知あり、SLAAC有効
     * PD系VLAN群（②④）：プレフィックス通知なし、DHCPv6誘導のみ

3. **DHCPv6サーバー**（`./kea-dhcp6/`）
   * `kea-dhcp6.conf` — Kea設定
     * subnet6（SLAAC動的）— プールなし、Information-Request応答のみ。Option 94はSubnet直下のflex-option式で付与（DNS, NTP, domain-search）
     * subnet6（SLAAC固定 各VLAN）— プールなし。`map-e-static-ip.conf`登録済みMACはHost Reservationの`option-data`でOption 94を個別固定（顧客指定IPv4を含む完全hex）。未登録MACは`UNKNOWN`クラスのoption-data（①と同一のflex-option式）にフォールバック
     * subnet6（PD動的）— `/40`プール（`PD_POOL`）から`/56`ずつ動的委譲、Option 94はSubnet直下のflex-option式で付与
     * subnet6（PD固定）— `/56`はHost Reservationで固定払い出しせず、**PD動的と同一の`PD_POOL`**から都度動的に委譲する（Keaのプール内重複回避保証に相乗り）。`map-e-static-ip.conf`登録済みMACはHost Reservationの`option-data`でOption 94を個別固定（顧客指定IPv4＋`PD_FIX_POOL`由来のダミーIPv6を含む完全hex）。未登録MACは`UNKNOWN`クラスのoption-data（②と同一のflex-option式）にフォールバック
    * hooks-libraries: `libdhcp_flex_option.so`（①②のSubnet直下、および③④の`UNKNOWN`クラスにOption 94のflex-option式を`supersede`、[13章](#13-flex-option-詳細仕様)）＋ `libdhcp_run_script.so`（`kea-map-e-hook`、ルーティング注入・削除用）
     * reservations: ③④の`map-e-static-ip.conf`登録MACごとに、Option 94の完全固定hexを`option-data`として持つHost Reservationを生成（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）
     * ※複数VLANインターフェースを単一のPDプールに紐付けるため、Keaのshared-networks設定を使用する
  * `kea-map-e-hook` — DHCPv6リースイベントスクリプト（ルーティング注入・削除用）
     * `leases6_committed`: IA_PD確定時にIPv6ルート＋ip6tnlトンネル＋IPv4ルートを自動注入（IPv4は①②なら[5章](#5-ipv4アドレス算出ロジックflex-optionhost-reservation併用方式)の式、③④なら`map-e-static-ip.conf`のMAC引き当て結果をPerl/Pythonで再実装して算出。Kea側の結果と一致させる）
     * `lease6_release`/`lease6_expire`: トンネル・ルートの自動削除
     * MACアドレスからリンクローカルIPv6を逆引き（NDPキャッシュ→EUI-64フォールバック）
   * `kea.sudoers` — `_kea` ユーザーに `ip` コマンドのNOPASSWD権限

4. **AppArmorプロファイル**（`./apparmor/`）
   * `usr.sbin.kea-dhcp6`, `usr.sbin.kea-lfc` — Kea用（テンプレートなし、変更なし）

5. **DNS (BIND9)**（`./bind/`）
   * `named.conf.local` — 2つのゾーン定義
   * `db.map.ocn.ad.jp` — OCN MAP-Eゾーン（`@`, `rule`, `*` → BR IPv6アドレス）
   * `db.v6connect.net` — v6connectゾーン（`@`, `*` → BR IPv6アドレス）
   * CEルーターがDNSを引いてprovisioning-serverのIPv6アドレスを解決する

6. **NTP (chrony)**（`./chrony/`）
   * `chrony.conf` — `map-e.conf` の `SYSTEM_NTP` からサーバーリスト生成、全IF/全ネットワークからのクライアント許可

7. **MAP-E 設定配布・ルーティング**（`./mape-provisioning-server/`）
   * `mape-provisioning-server` — HTTPSサーバー（Python3, Port 443）
     * CEからのGETリクエストに応答、JSON形式でMAP-Eルール（basicMapRules）を返却
     * 4つの認証パス: SLAAC（動的・固定）／DHCPv6-PD（動的・固定）
     * 送信元IPv6 → Keaリースファイル逆引き → MACアドレス特定
     * ①②（動的）：IPv4は[5章](#5-ipv4アドレス算出ロジックflex-optionhost-reservation併用方式)の式（`mape_calc`）で算出
     * ③④（固定）：MACをキーに`map-e-static-ip.conf`を検索し、登録済みの固定IPv4をそのまま採用（未登録の場合は①②と同じ`mape_calc`の式にフォールバック）
     * ルーティング自動設定: ip6tnlトンネル作成＋IPv4ルート＋NDPキャッシュ静的登録
     * テスト用アクセス手順: `curl` コマンド等によるテストアクセス手順の詳細については [付録C](#付録c-mape-provisioning-server-テストアクセス手順curl等) を参照
   * `mape-route-monitor` — NDP監視デーモン（Python3）
     * `ip monitor neigh` でPD系VLAN上の近隣イベントを監視
     * グローバルIPv6アドレスのREACHABLE/STALE/DELAY検出時にIPv4ルートを動的注入
   * `mape_calc` — 共通計算ライブラリ（[5章](#5-ipv4アドレス算出ロジックflex-optionhost-reservation併用方式)のロジックをPerl/Pythonで再実装。Kea側flex-option式・Host Reservation個別指定と同一の結果になることをテストで担保する）
     * `calc_map_e_params()`: IPv6プレフィックス → IPv4＋CE用IPv6算出
     * `calc_ipv4_from_pd_prefix()`: 払い出された`/56`プレフィックスの委譲ID部分（バイトオフセット固定）を抜き出してIPv4を算出（PD動的）
     * `calc_ipv4_from_vlan_and_duid()`: VLAN番号＋DUIDの一部からIPv4を算出（SLAAC動的）
     * `lookup_static_ipv4(mac)`: `map-e-static-ip.conf`からMACに対応する固定IPv4を検索（③④、未ヒット時はNoneを返し動的算出へフォールバック）
     * `get_target_ipv4_and_dev_from_host_part()`: CE IPv6 → 埋め込みIPv4逆算
    * `get_tunnel_dev_name()`: IPv4 → トンネルデバイス名（`mpe-common`）
     * `convert_to_data_ipv6()`: プレフィックス＋IPv4 → データ通信用CE IPv6合成
   * サービスファイル（systemd）
     * `mape-provisioning-server.service` — HTTPSサーバーの自動起動
     * `mape-route-monitor.service` — NDP監視デーモンの自動起動
   * SSL証明書（自己署名）
     * CN/SAN: `*.ocn.ad.jp`（有効期限3650日）

8. **固定IPv4マッピング**（`./map-e-static-ip.conf`）
   * フォーマット: `MACアドレス,固定IPv4/プレフィックス長`（カンマ区切り＋CIDR表記。`gen_kea_configs.pl` や `mape_calc` 側でカンマ分割および `/mask` 除去を行って固定IPv4を取得する。IPv6アドレスは変動するため記載不要。）
   * `kea-dhcp6.conf`のHost Reservation生成（`gen_kea_configs.pl`）が本ファイルを読み込み、MACごとにOption 94の固定hexを事前計算した`reservations`エントリを組み立てる（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）
   * `mape-provisioning-server`／`mape-route-monitor`も同じファイルを参照し、Kea側と同一の固定IPv4をルーティング注入に用いる
   * 固定IPv4自体の重複回避はKeaの機構に相乗りできないため、登録内容の重複がないことは運用者が担保する

9. **CEルーター設定例**（`./CE-setting`）
   * NEC IX2215（IOS: 10.10.29）のrunning-config
   * `tunnel mode map-e ocn-fixed` によるMAP-Eトンネル構成
   * GigaEthernet0.0（WAN/SLAAC）, GigaEthernet1.0（LAN10）, GigaEthernet2.0（LAN20）
   * DHCPv6 Information-request（DNS, NTP, domain-search）
   * IPv6 NDプロキシ（GE0→GE1）

---

## 12. map-e.conf パラメータ一覧

### 12.1 管理系（共通）

| 変数名 | 値（例） | 説明 |
|---|---|---|
| `MGT_IF` | `ens18` | 管理用インターフェース |
| `MGT_IP` | `192.168.0.42` | 管理用IPv4 |
| `MGT_MASK` | `24` | 管理用サブネットマスク |
| `MGT_GW` | `192.168.0.254` | 管理用ゲートウェイ |
| `SYSTEM_DNS` | `1.0.0.1, 8.8.4.4, 1.1.1.1, 8.8.8.8` | システム用DNS（フォワーダー） |
| `SYSTEM_NTP` | `192.168.0.252, 192.168.0.253, 192.168.0.254` | システム用NTP |
| `DOMAIN` | `my.corp` | ドメイン名 |

### 12.2 BR内部インターフェース・MAP-E共通

| 変数名 | 値（例） | 説明 |
|---|---|---|
| `MAPE_IF` | `ens19` | MAP-E物理・トランクインターフェース |
| `BR_IF` | `dummy0` | トンネル終端インターフェース |
| `PROV_IF` | `dummy1` | 共通サービス基盤インターフェース |
| `BASE_SUBNET` | `5f00:3aa` | 基幹IPv6プレフィックス |
| `BR_PREFIX` | `${BASE_SUBNET}:a001` → `5f00:3aa:a001` | BRプレフィックス（96bit） |
| `BR_IPV4_ADDR` | `100.127.255.255` | BR IPv4（CEから見たGW、`BR_IPV6`合成に使用） |
| `BR_IPV6` | 自動生成（`BR_PREFIX` + `BR_IPV4_ADDR`の16進） | `5f00:3aa:a001::647f:ffff` |
| `BR_IPV4_POOL` | `1.1.0.0`（例） | 全方式（SLAAC動的/PD動的）共有のIPv4プール上位16bit。動的抽出した16bitをそのまま下位16bitとしてflex-optionで`concat`するため、**マスク長は`/16`固定**（[14章](#14-アドレスプール設計とflex-optionの実現性検証)、`/17`等バイト境界に一致しない値は不可） |
| `BR_IPV4_MASK` | `16` | 上記プールのマスク長（`/16`固定・変更不可） |
| `MAPE_PROV_PREFIX` | `${BASE_SUBNET}:9999` → `5f00:3aa:9999` | 共通サービス用プレフィックス |
| `MAPE_DNS_IP` | `${MAPE_PROV_PREFIX}::53` | 全方式共通のDNSアドレス |
| `MAPE_NTP_IP` | `${MAPE_PROV_PREFIX}::123` | 全方式共通のNTPアドレス |
| `MAPE_PROV_IP` | `${MAPE_PROV_PREFIX}::ff` | provisioning-serverアドレス |
| `MAPE_DOMAIN_SEARCH` | `map.ocn.ad.jp` | CEへ通知するドメインサーチリスト |

### 12.3 ① SLAAC動的

| 変数名 | 値（例） | 説明 |
|---|---|---|
| `SLAAC_DYN_VLANS` | `901 903 905` | このVLANはSLAAC動的として扱う（空白区切りで任意個追加可） |
| `SLAAC_BR_BASE` | `1000` | IPv6プレフィックス4番目hextetの計算用ベース値（`${BASE_SUBNET}:${SLAAC_BR_BASE}+VLANID::/64`） |
| `SLAAC_BR_SUFFIX` | `::ff0a` | 各VLANのBR側アドレスのサフィックス |

### 12.4 ② DHCPv6-PD動的

| 変数名 | 値（例） | 説明 |
|---|---|---|
| `PD_DYN_VLANS` | `911 913 915` | このVLANはDHCPv6-PD動的として扱う |
| `PD_POOL` | `2000` | 委譲プール（`${BASE_SUBNET}:${PD_POOL}::/40` → `5f00:3aa:2000::/40`、VLANを区別せず全VLAN共有） |

`/40`からの委譲単位（`/56`）はスクリプト内の固定値（変更不可）であり、`map-e.conf` に個別の変数は持たない。

### 12.5 ③ SLAAC固定

| 変数名 | 値（例） | 説明 |
|---|---|---|
| `SLAAC_FIX_VLANS` | `921 923 925` | このVLANはSLAAC固定として扱う |
| `SLAAC_FIX_BR_PREFIX` | `3000` | IPv6プレフィックス4番目hextetの計算用ベース値（`${BASE_SUBNET}:${SLAAC_FIX_BR_PREFIX}+VLANID::/64`） |
| `SLAAC_FIX_BR_SUFFIX` | `::ff0a` | 各VLANのBR側アドレスのサフィックス（動的と同値だが、プレフィックス帯が異なるため衝突しない） |

### 12.6 ④ DHCPv6-PD固定

| 変数名 | 値（例） | 説明 |
|---|---|---|
| `PD_FIX_VLANS` | `931 933 935` | このVLANはDHCPv6-PD固定として扱う |
| `PD_FIX_POOL` | `4000` | Option 94のRule IPv6 Prefixに埋め込む**ダミー値**のネットワーク部（`${BASE_SUBNET}:${PD_FIX_POOL}::/56` → `5f00:3aa:4000::/56`、委譲ID部分は`0000`固定）。実際のIPv6 `/56`委譲には使用しない |

④のIPv6 `/56`はHost Reservationによる固定払い出しを行わず、**`PD_POOL`（②と同一のKea PDプールオブジェクト）から都度動的に採番する**。これにより、②④間でKeaのプール内重複回避保証がそのまま働き、IPv6重複を気にする必要がなくなる（[14章](#14-アドレスプール設計とflex-optionの実現性検証)参照）。固定運用の対象はIPv4のみであり、`PD_FIX_POOL`はOption 94のIPv6欄を埋めるためのダミー値としてのみ使われる点に注意。

### 12.7 map-e-static-ip.conf の例

MACアドレスと顧客指定の固定IPv4の対応表。カンマ区切りで `MACアドレス,固定IPv4/プレフィックス長` の形式で記述する（`gen_kea_configs.pl` や `mape_calc` スクリプト側でカンマ分割をパースし、`/mask` が付いている場合は自動でプレフィックス長を取り除いてIPv4を取得する）。IPv6（`/56`や`/64`）は記載しない（OCN商用でもIPv6アドレスは変動しうるため、固定運用の対象外とした）。③④共通で使用し、どちらのVLANに接続しても同じMACには同じIPv4が割り当てられる。

```
# MACアドレス,固定IPv4/プレフィックス長
00:a0:de:11:22:33,203.0.113.32/27
00:a0:de:44:55:66,198.51.200.1/32
```

`gen_kea_configs.pl`（[13章](#13-flex-option-詳細仕様)）が本ファイルを読み込み、MACごとにOption 94の完全な固定hex値（顧客指定IPv4を含む）を事前計算し、Kea Host Reservationの`option-data`として埋め込む（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）。③はVLAN（Subnet）ごとにIPv6部分が異なるためSubnet単位でreservationを複製し、④はダミー値で統一されているためshared-network直下に1回だけ登録すればよい。

### 12.8 自動導出・派生変数一覧（MapeCommon.pm で自動算出）

`map-e.conf` 内で直接定義せず、スクリプト実行時（`MapeCommon::load_conf_file`）に他の設定値から自動的に補完・算出される派生変数一覧。

| 変数名 | 算出ロジック | 値（例） | 説明 |
|---|---|---|---|
| `BR_IPV6` | `BR_PREFIX` + `BR_IPV4_ADDR`(16進) | `2400:4150:3220::647f:ffff` | BRの128bit完全IPv6アドレス |
| `MAPE_FLAT_RULE_IPV6_PREFIX` | `${BASE_SUBNET}::` | `2400:4150::` | MAP-E Rule IPv6 Prefix (Flat Mode用) |
| `MAPE_FLAT_RULE_IPV6_LEN` | `32` (BASE_SUBNETヘクステット数×16) | `32` | MAP-E Rule IPv6 Prefix Length |
| `MAPE_PD_PREFIX` | `${BASE_SUBNET}:${PD_POOL}:` | `2400:4150:2000:` | DHCPv6-PD動的配布用プレフィックス |
| `MAPE_PD_FIX_PREFIX` | `${BASE_SUBNET}:${PD_FIX_POOL}:` | `2400:4150:4000:` | DHCPv6-PD固定用プレフィックス |

---

## 13. flex-option 詳細仕様

`kea-dhcp6` に `libdhcp_flex_option.so` をロードし、**RFC 7598 準拠 Option 94（OPTION_S46_CONT_MAPE）** の値を、動的接続方式（①②）についてはSubnet／Client Class単位の式（expression）で組み立てる。固定接続方式（③④）はflex-optionを使わず、Host Reservationごとの`option-data`に事前計算済みの完全な固定hexを直接設定する（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）。Kea設定ファイル自体がSubnet／Class／Reservationごとに静的な値・式を持つ。

### 13.1 hooks-libraries設定の全体構造

```jsonc
{
  "Dhcp6": {
    "hooks-libraries": [
      { "library": "libdhcp_flex_option.so",
        "parameters": {
          "options": [
            {
              "code": 94,
              "supersede": "<Subnet固有の式。①②はSubnet直下、③④はUNKNOWNクラスに設定。13.3節参照>",
              "csv-format": false,
              "client-class": "<VLAN/接続方式を表すクラス名、または③④ならUNKNOWN>"
            }
          ]
        }
      },
      { "library": "libdhcp_run_script.so",
        "parameters": { "name": "/usr/local/bin/kea-map-e-hook", ... }
      }
    ]
  }
}
```

`libdhcp_flex_option.so`の式は**クライアントがそのリクエストパケットで送ってきた内容（クエリパケット）にのみアクセスできる**（[4.0節](#40-方式概要)の制約）。`kea-dhcp6.conf.tmpl`の生成時（ [gen_kea_configs.pl](file:///home/abc123/map-e_ocn/generator/gen_kea_configs.pl) ）に、VLANごとの固定部分（BR IPv6、IPv4上位バイト、prefix6-len等）をあらかじめ置換処理で埋め込み、式自体はSubnetごとに完全に静的な文字列として出力する。

なお、`gen_kea_configs.pl` が生成する Option 94（Container Option: `OPTION_S46_CONT_MAPE`）内には、Option 90（BR Option: `0x005a...`）および Option 89（Rule Option: `0x0059...`）に加え、動的セグメントでは末尾に Option 93（Port Parameters Option: `OPTION_S46_PORTPARAMS`: code 93, `0x005d...`、offset-len 4bit + psid-len 8bit + PSID 16bit）が結合されて送出される。

### 13.2 PD動的（②）の式・PD固定（④）のUNKNOWNフォールバック式

`option[25].option[26].hex`はIAPREFIXサブオプションの生データ25byte（preferred-lifetime 4B＋valid-lifetime 4B＋prefix-length 1B＋prefix address 16B）。`/40→/56`の委譲ID（16bit＝2byte）は、prefix address内のバイトオフセット5〜6（＝IAPREFIXデータ全体ではオフセット14、長さ2）に**必ずバイト境界で一致する**（[14.1節](#141-40-56委譲がバイト境界に一致する根拠)で検証）。

```
supersede:
  concat(
    <BR_IPV6の16byte hex 固定値>,                 // OPTION_S46_BR (type90, len16)
    concat(
      <flags(0x00)+ea-len(0x08)+prefix4-len(0x20) 固定3byte>,  // OPTION_S46_RULE ヘッダ
      concat(
        <BR_IPV4_POOL上位2byte 固定hex>,
        concat(
          substring(option[25].option[26].hex, 14, 2),   // Keaが払い出した/56の委譲ID(2byte)をそのまま流用
          concat(
            <prefix6-len(0x38=56)+IPv6prefix7byte 固定/Subnet別hex>,
            concat(
              <OPTION_S46_PORTPARAMS ヘッダ (0x005d0004040800) 7byte>, // Option 93 (offset-len 4, psid-len 8)
              substring(option[25].option[26].hex, 13, 1) // target_psid (1byte)
            )
          )
        )
      )
    )
  )
```

この式は②（PD動的）のSubnetに直接設定するほか、④（PD固定）のSubnet／shared-networkに紐付けた`UNKNOWN`クラスにも同一の式を設定する（`map-e-static-ip.conf`未登録MACのフォールバック用）。②④はいずれもKeaの同一PDプールオブジェクト（`PD_POOL`）から`/56`を払い出すため、Keaのプール内重複回避保証がそのまま働く。

### 13.3 SLAAC動的（①）の式・SLAAC固定（③）のUNKNOWNフォールバック式

SLAACにはKeaによるアドレス払い出し（IA_NA）が存在しないため、PD系のような「payload由来の重複回避保証済みバイト列」が使えない。そこで、VLAN番号（Subnet生成時に固定値として埋め込み）とクライアントDUID（`option[1].hex`、Client Identifier）の一部を組み合わせる。

```
supersede:
  concat(
    <BR_IPV6の16byte hex 固定値>,
    concat(
      <flags(0x00)+ea-len(0x08)+prefix4-len(0x20) 固定3byte>,
      concat(
        <BR_IPV4_POOL上位2byte 固定hex>,
        concat(
          concat(<このVLAN番号を表す1byte固定hex>, substring(option[1].hex, -1, 1)),  // 下位1byteはDUID末尾1byte
          concat(
            <prefix6-len(0x40=64)+IPv6prefix8byte 固定/Subnet別hex>,
            <OPTION_S46_PORTPARAMS(type93, offset-len=4, psid-len=8, PSID) 6byte (0x005d0004040800 + psid_hex)>
          )
        )
      )
    )
  )
```

この式は①（SLAAC動的）のSubnetに直接設定するほか、③（SLAAC固定）のSubnetに紐付けた`UNKNOWN`クラスにも同一の式を設定する（`map-e-static-ip.conf`未登録MACのフォールバック用）。VLAN番号部分は設計上1VLAN=1CEであるため単独でも一意だが、DUID末尾1byteを追加することでVLANの再利用や複数CE収容時の衝突耐性を高める（ただしKeaによる重複回避保証があるわけではない点は[5章](#5-ipv4アドレス算出ロジックflex-optionhost-reservation併用方式)のとおり）。

### 13.4 SLAAC固定（③）・PD固定（④）：登録済みMACのOption 94個別指定

③④で`map-e-static-ip.conf`に登録されたMACは、Kea Host Reservationの`option-data`としてOption 94の完全な固定hexが直接設定される（式評価は行わない）。

```jsonc
{
  "hw-address": "<登録MAC>",
  "option-data": [
    {
      "code": 94,
      "csv-format": false,
      "data": "<下記の内訳で事前計算した完全固定hex文字列>"
    }
  ]
}
```

hexの内訳（`gen_kea_configs.pl`が`map-e-static-ip.conf`とVLANごとの固定値から計算）：

```
data:
  concat(
    <BR_IPV6の16byte hex 固定値>,                          // OPTION_S46_BR
    concat(
      <flags(0x00)+ea-len(0x00)+prefix4-len(0x20) 固定3byte>,  // OPTION_S46_RULE ヘッダ
      concat(
        <map-e-static-ip.confの固定IPv4を16進変換した4byte>,     // 顧客指定IPv4
        <prefix6-len+IPv6prefix 固定hex>                         // ③:実VLANプレフィックス／④:PD_FIX_POOLダミー値
      )
    )
  )
```

③・④とも「顧客指定IPv4を含めて完全に静的なhex文字列」である点が①②（および③④のUNKNOWNフォールバック）のflex-option式と異なる。IPv6部分の扱いは③（実在のVLANプレフィックス、Subnetごとにreservationを複製）と④（`PD_FIX_POOL`由来のダミー値、shared-network直下に1回登録すれば使い回し可）で異なる（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)参照）。

### 13.5 ea-len・prefix4-len等の固定値

| 接続方式 | flags | ea-len | prefix4-len | prefix6-len | Option 94の生成方式 |
|---|---|---|---|---|---|
| ①SLAAC動的 | `0x00` | `0x08` | `0x20`(32) | `0x40`(64) | flex-option式（Subnet直下） |
| ②PD動的 | `0x00` | `0x08` | `0x20`(32) | `0x38`(56) | flex-option式（Subnet直下） |
| ③SLAAC固定・登録済み | `0x00` | `0x00` | `0x20`(32) | `0x40`(64) | Host Reservation個別`option-data`（完全固定hex） |
| ③SLAAC固定・未登録 | `0x00` | `0x08` | `0x20`(32) | `0x40`(64) | flex-option式（①と同一、`UNKNOWN`クラス） |
| ④PD固定・登録済み | `0x00` | `0x00` | `0x20`(32) | `0x38`(56) | Host Reservation個別`option-data`（完全固定hex、IPv6部分はダミー） |
| ④PD固定・未登録 | `0x00` | `0x08` | `0x20`(32) | `0x38`(56) | flex-option式（②と同一、`UNKNOWN`クラス） |

これらはSubnet／Class／Reservationごとに、Kea設定ファイル上へ静的に書き込まれる値。

---

## 14. アドレスプール設計とflex-optionの実現性検証

本章は「PD動的`/17`・SLAAC動的`/17`」というプール設計案がflex-optionで実現可能かどうかの検証結果（14.1〜14.3節）と、③④(固定接続方式)が①②(動的接続方式)へフォールバックする際の重複回避条件（14.4節）をまとめたものである。なお2026-07再改訂により、③④の固定IPv4自体はflex-optionを使わずHost Reservation個別`option-data`で直接指定する方式に変更されており（[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）、本章のflex-option制約はもっぱら①②（動的）および③④の未登録MACフォールバック時の議論に関わる。

### 14.1 `/40→/56`委譲がバイト境界に一致する根拠

IPv6アドレスは128bit=16byteで、`/40`は40bit=**ちょうど5byte**、`/56`は56bit=**ちょうど7byte**であるため、両者とも8の倍数bitでバイト境界に一致する。したがって「`/40`プール内で可変となる`/56`の委譲ID部分」は、prefix address（16byte）のうちオフセット5〜6の**ちょうど2byte（16bit）**であり、`substring(..., offset, 2)`のような整数バイトオフセット指定で過不足なく取り出せる。これはKeaの式言語がバイト単位の`substring`/`concat`しか提供しない（14.2節）という制約と、完全に整合している。

### 14.2 flex-option式言語の制約（確認結果）

Kea公式ドキュメント（Client Classification / Flexible Option Hooks Library）を確認した結果、式言語で利用できる関数は次のみである。

* 文字列操作：`substring(value, start, length)`、`concat(a, b)`、`split(value, delimiters, field)`、`ifelse(cond, a, b)`、`hexstring(value, sep)`
* 変換：`int8totext`/`int16totext`/`int32totext`/`uint8totext`/`uint16totext`/`uint32totext`（数値↔文字列変換であり、四則演算ではない）
* 存在判定・比較：`option[x].exists`、`==`など

**加算・減算・剰余（mod）・ビットシフト・ビットマスクといった算術演算は一切提供されない。** つまりflex-optionでできるのは「クエリパケット中の既存バイト列を、そのまま切り出して並べ替える」ことだけであり、「任意サイズのプールへ値を分散させる」処理は表現できない。

### 14.3 結論：`/17`プールは直接実現できない

* `/16`プール（host部16bit＝2byte）：14.1節の`/40→/56`委譲ID（2byte）と**完全にバイト境界が一致**するため、`substring`＋`concat`だけで過不足なく・重複なく埋め込める。**実現可能。現行案を`/16`に変更することを推奨する。**
* `/17`プール（host部15bit）：15bitは1byteの倍数ではなく、バイト境界に一致しない。flex-optionには算術演算もビットマスクもないため、2byteの委譲ID（16bit）から特定の1bitだけを落とす、あるいは折り込む（fold）といった処理は**実現不可能**である。無理に`/17`のまま`substring`で2byteをそのまま埋め込むと、IPv4プールの外側（`/17`の範囲外）にはみ出したアドレスが生成されてしまい、ルーティング設計と矛盾する。

**推奨対応**：PD動的・SLAAC動的とも、プールサイズは`/17`ではなく**`/16`**とする（[12.2節](#122-br内部インターフェースmap-e共通)の`BR_IPV4_POOL`）。どうしても`/17`など8の倍数でないプール長が必要な場合は、flex-optionではなく外部スクリプト（`libdhcp_run_script.so`経由）でIPv4を計算する方式に戻す必要があり、その場合はKeaの重複回避保証への相乗りが薄れる。

### 14.4 固定接続方式のIPv6委譲・フォールバック時の重複回避条件

2026-07再改訂により、③④の**固定IPv4自体**はHost Reservation個別`option-data`で顧客指定値をそのまま設定するため、flex-option式のバイト抽出ロジックには依存しない（IPv4重複の回避は運用者が`map-e-static-ip.conf`の登録内容を管理することで担保する。[4.5節](#45-固定接続方式③④のoption-94host-reservation個別指定)）。

一方、④(PD固定)のIPv6 `/56`委譲についてはHost Reservationでの固定払い出しをやめ、**②(PD動的)と同一のKea PDプールオブジェクト（`PD_POOL`）から都度動的に採番する**設計とした。②④が同一のKeaプールオブジェクトを共有することで、Kea自身が単一プール内で重複回避するため、IPv6 `/56`の重複は生じない。これは③④の未登録MACが①②のflex-option式へフォールバックする際にも同様で、フォールバック先が動的側と同一プールであるため、flex-option式（委譲IDの2byteをそのまま切り出すだけ）でも安全にIPv6重複を防げる。

SLAAC固定（③）→SLAAC動的（①）のフォールバックについても、SLAAC系はもともとKeaによる払い出しが存在せず、VLAN番号（Subnet単位で一意）を主要な衝突回避要素としているため、③・①のVLAN番号が重複しない限り（＝[2章](#2-wanインターフェースvlan設計)の設計どおり1VLAN=1CEが守られている限り）実質的な衝突リスクは小さい。

### 14.5 まとめ

| 項目 | 実現可否 | 備考 |
|---|---|---|
| PD動的の重複回避へのflex-option相乗り | ○ 実現可能 | `/40→/56`がバイト境界一致のため`substring`のみで安全 |
| ④のIPv6 `/56`委譲（②と同一プール共有） | ○ 実現可能 | ②④が同一Kea PDプールオブジェクトを共有するため重複なし |
| ③④の固定IPv4（顧客指定値） | ○ 実現可能 | flex-optionではなくHost Reservation個別`option-data`で直接指定。重複回避はKeaに相乗りできないため運用者が`map-e-static-ip.conf`を管理 |
| ③④未登録MACのUNKNOWNフォールバック | ○ 実現可能 | ①②と同一のflex-option式・同一プールへ収束するため追加対策は不要 |

---

## 付録A. CEルーター設定リファレンス

IX ルーターの設定は、ひかり電話の有無により2パターンを使用する。

**動的IPv4アドレスの場合**
- <https://jpn.nec.com/univerge/ix/Support/ipv6/OCN-VC/index.html#ipv6ra>
- <https://jpn.nec.com/univerge/ix/Support/ipv6/OCN-VC/index.html#dhcpv6pd>

**固定IPv4 1アドレスの場合**
- <https://jpn.nec.com/univerge/ix/Support/ipv6/OCN-VC/static-ip1.html#ipv6ra>
- <https://jpn.nec.com/univerge/ix/Support/ipv6/OCN-VC/static-ip1.html#dhcpv6pd>

**固定IPv4複数アドレスの場合**
- <https://jpn.nec.com/univerge/ix/Support/ipv6/OCN-VC/static-ip8.html#ipv6ra>
- <https://jpn.nec.com/univerge/ix/Support/ipv6/OCN-VC/static-ip8.html#dhcpv6pd>

**DHCPv6クライアントプロファイル**（NTP・ドメインサーチリストを追加）
```
ipv6 dhcp client-profile dhcpv6-cl
  option-request dns-servers
  option-request domain-search-list
  option-request ntp-servers
```

**ファームウェア更新に伴う設定変更**
```
interface GigaEthernet0.0
 no ipv6 address                              # 削除
 ipv6 address autoconfig receive-default       # 代わりに追加
```

YAMAHA RTX の設定例
https://www.rtpro.yamaha.co.jp/RT/docs/ocn_map_e/index.html#setting1

Fortigate の設定例
https://mytech-blog.com/fortigate-ipoe/#index_id7

---

## 付録B. ログ方針

ログは、出力できない特別な事情がない限り syslog へ出力する。

---

## 付録C. mape-provisioning-server テストアクセス手順（curl等）

`mape-provisioning-server`（HTTPS / Port 443）の動作確認やレスポンス検証を、CEルーターの実機を使わずに `curl` コマンド等でテストアクセスする手順を以下に示す。

### C.1 前提要件と注意点

* **HTTPS / 自己署名証明書**: サーバーは自己署名証明書（`server.crt`）で動作しているため、`curl` の `-k`（`--insecure`）オプションが必要。
* **IPv6 アドレス表記**: `curl` で IPv6 生アドレスを指定する場合は `-g`（`--globoff`）オプションを付与し、`[5f00:3aa:9999::ff]` のように角括弧で囲む。
* **プロビジョニングサーバーアドレス**: `${MAPE_PROV_IP}`（デフォルト: `5f00:3aa:9999::ff`）。BINDが動作している場合はドメイン名 `map.ocn.ad.jp` や `v6connect.net` でもアクセス可能。
* **自動ルーティング注入の発生**: テストアクセスを行うと、サーバーは指定された接続元IPv6または `ipv6Prefix` に応じたIPv4ルート（`ip route add <IPv4>/32 dev mpe-common encap ip6...`）をBRのカーネルテーブルへ自動設定する。

### C.2 テストアクセス用 curl コマンド例

#### 1. 基本疎通・動作確認 (SLAAC動的/固定)
SLAAC環境（①③）ではCEがグローバルIPv6アドレスからリクエストを送信するため、指定インターフェース経由またはグローバルIPv6送信元アドレスを指定してアクセスする。

```bash
# 共通サービスアドレス (5f00:3aa:9999::ff) へ直接アクセス
curl -k -g -6 "https://[5f00:3aa:9999::ff]/"

# ドメイン名指定でアクセス (DNS解決確認兼)
curl -k -g -6 "https://map.ocn.ad.jp/"

# 特定のVLANインターフェース (例: ens19.901) を送信元として指定
curl -k -g -6 --interface ens19.901 "https://[5f00:3aa:9999::ff]/?ipv6Prefix=::&ipv6PrefixLength=0"
```

#### 2. DHCPv6-PD環境のエミュレーション (ipv6Prefix クエリパラメータ付与)
DHCPv6-PD環境（②④）では、CEとBR間の対向通信がリンクローカルアドレス（`fe80::`）で行われるため、CEは委譲された `/56` プレフィックスを `ipv6Prefix` クエリパラメータでサーバーに通知する。

```bash
# 委譲プレフィックス (例: 5f00:3aa:2000:100::) を指定してアクセス
curl -k -g -6 "https://[5f00:3aa:9999::ff]/?ipv6Prefix=5f00:3aa:2000:100::&ipv6PrefixLength=56"
```

#### 3. 応答JSONフォーマットの確認 (jq 連携)
`curl` に `-s`（silent）と `jq` を組み合わせることで、整形されたJSONレスポンスを確認できる。

```bash
curl -sk -g -6 "https://[5f00:3aa:9999::ff]/?ipv6Prefix=5f00:3aa:2000:100::" | jq .
```

**正常応答例 (動的接続・SLAAC/PD)**:
```json
{
  "basicMapRules": [
    {
      "brIpv6Address": "5f00:3aa:a001::647f:ffff",
      "ipv6Prefix": "5f00:3aa::",
      "ipv6PrefixLength": 32,
      "ipv4Prefix": "198.51.159.68",
      "ipv4PrefixLength": 32,
      "eaBitLength": 8,
      "psIdOffset": 4,
      "psId": 51
    }
  ]
}
```

### C.3 実行結果・ログおよびルーティングの検証

テストアクセス実行後、以下のコマンドでサーバーログおよびBR側自動ルーティングの反映状態を確認する。

1. **サーバー側ログの確認** (`syslog` / `journalctl`):
   ```bash
   journalctl -u mape-provisioning-server -n 20 --no-pager
   ```
   *ログ出力例*:
   `Request received - URL: /?ipv6Prefix=5f00:3aa:2000:100:: from Client-IP: fe80::...`
   `Payload to Router: {"basicMapRules": [...]}`

2. **自動注入ルートの確認** (`ip route`):
   ```bash
   ip route show dev mpe-common
   ```
   *出力例*:
   `198.51.159.68 via ... dev mpe-common encap ip6 src 5f00:3aa:a001::647f:ffff dst 5f00:3aa:2000:100:c6:339f:4400:0`
